# 12 Linux 常用命令与日志排查：从「SSH 上去一脸懵」到能独立查故障 · 开发面试题

> **知识来源**：[12.linux-commands-log-tutorial.md](../12.linux-commands-log-tutorial.md)（复习用，面试不考「是否读过」）  
> **题量**：17 道  
> **岗位**：后端 / AI 应用 / RAG·Agent 工程开发  
> **建议**：先口头作答，再对照「参考答法」；重点练 **设计、排障、trade-off**，不是背文档目录。

---

## 题目目录

1. [什么是 Linux？在 从「SSH 上去一脸懵」到能独立查故障 相关系统里它承担什么职责？](#q1)（概念题）
2. [什么是 WSL2？在 从「SSH 上去一脸懵」到能独立查故障 相关系统里它承担什么职责？](#q2)（概念题）
3. [什么是 Shell？在 从「SSH 上去一脸懵」到能独立查故障 相关系统里它承担什么职责？](#q3)（概念题）
4. [什么是 选项？在 从「SSH 上去一脸懵」到能独立查故障 相关系统里它承担什么职责？](#q4)（概念题）
5. [在 从「SSH 上去一脸懵」到能独立查故障 相关开发中，用 / / / / 在服务器上找到并阅读日志。 2. 用 / / 判断服务有没有…](#q5)（实现题）
6. [在 从「SSH 上去一脸懵」到能独立查故障 相关开发中，**不必一次背会所有命令**：本文是**地图 + 套路**；真机上多练几次 和 …](#q6)（实现题）
7. [在 从「SSH 上去一脸懵」到能独立查故障 相关开发中，**前置阅读**：会打开终端即可；若已读过 Docker Compose 教程，…](#q7)（实现题）
8. [在 从「SSH 上去一脸懵」到能独立查故障 相关开发中，**SSH 补充**： 登录远程服务器； 把本机文件拷到服务器。排错时常用 SS…](#q8)（实现题）
9. [如何设计/实现「权限与用户：为什么 Permission denied」？（从「SSH 上去一脸懵」到能独立查故障）](#q9)（设计题）
10. [如何设计/实现「数字权限（了解即可）」？（从「SSH 上去一脸懵」到能独立查故障）](#q10)（设计题）
11. [如何设计/实现「案例三：权限不足读不了日志」？（从「SSH 上去一脸懵」到能独立查故障）](#q11)（设计题）
12. [如何设计/实现「陷阱一：生产环境乱用 rm -rf」？（从「SSH 上去一脸懵」到能独立查故障）](#q12)（设计题）
13. [Linux 和 WSL2 有什么区别？在 从「SSH 上去一脸懵」到能独立查故障 里如何选型？](#q13)（对比题）
14. [场景题：以前的痛苦 → 学完后的做法… —— 在 从「SSH 上去一脸懵」到能独立查故障 架构下你怎么处理？](#q14)（场景题）
15. [场景题：SSH 上去不知道进哪个目录 → / / 三板斧… —— 在 从「SSH 上去一脸懵」到能独立查故障 架构下你怎么处理？](#q15)（场景题）
16. [从「SSH 上去一脸懵」到能独立查故障 能力要上生产，你会优先保证哪三件事？](#q16)（生产化）
17. [用 2 分钟讲清楚 从「SSH 上去一脸懵」到能独立查故障 的技术方案（面向面试官）。](#q17)（综合题）

---

### Q1. 什么是 Linux？在 从「SSH 上去一脸懵」到能独立查故障 相关系统里它承担什么职责？ {#q1}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Linux |

**参考答法（开发向）：**

**Linux**（这里指服务器上常见的发行版，如 Ubuntu、Debian、CentOS）：一种操作系统，服务器领域极其常见。

直觉：云主机里跑的那套「管家系统」——你不直接摸鼠标，而是打字给它下指令。

**工程语境（从「SSH 上去一脸懵」到能独立查故障）**：
- 解决什么：把「Linux」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Linux 和容易混淆的替代方案怎么选？
- 如果去掉 Linux 这一层，最先坏掉的是哪一环？

---

### Q2. 什么是 WSL2？在 从「SSH 上去一脸懵」到能独立查故障 相关系统里它承担什么职责？ {#q2}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · WSL2 |

**参考答法（开发向）：**

**WSL2**（Windows Subsystem for Linux）：在 Windows 里跑一个真实的 Ubuntu 子系统。

直觉：在 Windows 桌面上开一扇通往 Linux 世界的门。

**工程语境（从「SSH 上去一脸懵」到能独立查故障）**：
- 解决什么：把「WSL2」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- WSL2 和容易混淆的替代方案怎么选？
- 如果去掉 WSL2 这一层，最先坏掉的是哪一环？

---

### Q3. 什么是 Shell？在 从「SSH 上去一脸懵」到能独立查故障 相关系统里它承担什么职责？ {#q3}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Shell |

**参考答法（开发向）：**

**Shell**（壳）：你和 Linux 内核之间的「翻译官」，读你输入的文字命令并执行。

直觉：饭店服务员——你点菜（敲命令），厨房（系统）去做。

**工程语境（从「SSH 上去一脸懵」到能独立查故障）**：
- 解决什么：把「Shell」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Shell 和容易混淆的替代方案怎么选？
- 如果去掉 Shell 这一层，最先坏掉的是哪一环？

---

### Q4. 什么是 选项？在 从「SSH 上去一脸懵」到能独立查故障 相关系统里它承担什么职责？ {#q4}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · 选项 |

**参考答法（开发向）：**

**选项**（option）：以 或 开头，改行为（ 长格式、 含隐藏文件）

**工程语境（从「SSH 上去一脸懵」到能独立查故障）**：
- 解决什么：把「选项」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- 选项 和容易混淆的替代方案怎么选？
- 如果去掉 选项 这一层，最先坏掉的是哪一环？

---

### Q5. 在 从「SSH 上去一脸懵」到能独立查故障 相关开发中，用 / / / / 在服务器上找到并阅读日志。 2. 用 / / 判断服务有没有在跑、端口有没有在听。 3. 用 和 两套路径定位日志来源。 4. 按「六步法」从 502 / 磁盘满  {#q5}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：用 / / / / 在服务器上找到并阅读日志。 2. 用 / / 判断服务有没有在跑、端口有没有在听。 3. 用 和 两套路径定位日志来源。 4. 按「六步法」从 502 / 磁盘满 / 进程挂掉等现象查到可行动的原因。 5. 在 WSL 或云主机上完成 §16.8 跟练清单，建立「SSH 不慌」的基本手感

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q6. 在 从「SSH 上去一脸懵」到能独立查故障 相关开发中，**不必一次背会所有命令**：本文是**地图 + 套路**；真机上多练几次 和 ，比死记硬背更有效。建议把本文和 Docker Compose 教程 放在一起读：容器化解决环境一致，S {#q6}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：**不必一次背会所有命令**：本文是**地图 + 套路**；真机上多练几次 和 ，比死记硬背更有效。建议把本文和 Docker Compose 教程 放在一起读：容器化解决环境一致，SSH + 日志解决线上出事后的第一小时

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q7. 在 从「SSH 上去一脸懵」到能独立查故障 相关开发中，**前置阅读**：会打开终端即可；若已读过 Docker Compose 教程，§13 容器日志可直接接上——你会如何实现或验证？ {#q7}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：**前置阅读**：会打开终端即可；若已读过 Docker Compose 教程，§13 容器日志可直接接上

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q8. 在 从「SSH 上去一脸懵」到能独立查故障 相关开发中，**SSH 补充**： 登录远程服务器； 把本机文件拷到服务器。排错时常用 SSH；把日志文件 回本地用编辑器看也可以，但生产大日志优先在服务器 ——你会如何实现或验证？ {#q8}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：**SSH 补充**： 登录远程服务器； 把本机文件拷到服务器。排错时常用 SSH；把日志文件 回本地用编辑器看也可以，但生产大日志优先在服务器 

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q9. 如何设计/实现「权限与用户：为什么 Permission denied」？（从「SSH 上去一脸懵」到能独立查故障） {#q9}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 权限与用户：为什么 Permission |

**参考答法（开发向）：**

**背景要点**：在桌面上找「日志文件夹」——服务器没有桌面

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q10. 如何设计/实现「数字权限（了解即可）」？（从「SSH 上去一脸懵」到能独立查故障） {#q10}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 数字权限（了解即可） |

**参考答法（开发向）：**

**背景要点**：问同事「日志在哪」，对方说「看 nginx 和 journal」——你更懵了

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q11. 如何设计/实现「案例三：权限不足读不了日志」？（从「SSH 上去一脸懵」到能独立查故障） {#q11}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 案例三：权限不足读不了日志 |

**参考答法（开发向）：**

**背景要点**：**Shell**（壳）：你和 Linux 内核之间的「翻译官」，读你输入的文字命令并执行。 通俗说：饭店服务员——你点菜（敲命令），厨房（系统）去做。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q12. 如何设计/实现「陷阱一：生产环境乱用 rm -rf」？（从「SSH 上去一脸懵」到能独立查故障） {#q12}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 陷阱一：生产环境乱用 rm -rf |

**参考答法（开发向）：**

**背景要点**：常见 Shell 叫 **bash**（Bourne Again Shell）。提示符里 表示：用户 、主机 、当前在 home 目录 。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q13. Linux 和 WSL2 有什么区别？在 从「SSH 上去一脸懵」到能独立查故障 里如何选型？ {#q13}

| 维度 | 说明 |
|------|------|
| 类型 | 对比题 |
| 难度 | 中等 |
| 考点 | 对比 · Linux vs WSL2 |

**参考答法（开发向）：**

| 维度 | Linux | WSL2 |
|------|----------|----------|
| 定义 | 一种操作系统，服务器领域极其常见。 | 在 Windows 里跑一个真实的 Ubuntu 子系统。 |
| 适用 | 见具体场景约束 | 见具体场景约束 |

**选型建议**：
- 用 **输入形态**（精确词 vs 语义改写）、**延迟预算**、**运维成本** 三张表说话；
- 给出「默认选谁 + 何时叠加谁」，必要时 Hybrid；
- 用评测集证明，而不是团队偏好。

**常见追问：**

- 能否画一张两路召回合并的时序图？
- 线上如何做 A/B？

---

### Q14. 场景题：以前的痛苦 → 学完后的做法… —— 在 从「SSH 上去一脸懵」到能独立查故障 架构下你怎么处理？ {#q14}

| 维度 | 说明 |
|------|------|
| 类型 | 场景题 |
| 难度 | 中等 |
| 考点 | 业务场景 |

**参考答法（开发向）：**

**场景**：以前的痛苦 → 学完后的做法

**答题要点**：
- 先复述用户目标与约束（权限、延迟、是否允许拒答）；
- 给出 **数据流**：从请求进入到返回证据/答案；
- 点出 1～2 个容易翻车的配置（top_k、filter、超时）；
- 说明如何 **验收**（命中率、引用正确率、P95）。

**常见追问：**

- 如果证据不足，拒答还是让模型自由发挥？依据是什么？

---

### Q15. 场景题：SSH 上去不知道进哪个目录 → / / 三板斧… —— 在 从「SSH 上去一脸懵」到能独立查故障 架构下你怎么处理？ {#q15}

| 维度 | 说明 |
|------|------|
| 类型 | 场景题 |
| 难度 | 中等 |
| 考点 | 业务场景 |

**参考答法（开发向）：**

**场景**：SSH 上去不知道进哪个目录 → / / 三板斧

**答题要点**：
- 先复述用户目标与约束（权限、延迟、是否允许拒答）；
- 给出 **数据流**：从请求进入到返回证据/答案；
- 点出 1～2 个容易翻车的配置（top_k、filter、超时）；
- 说明如何 **验收**（命中率、引用正确率、P95）。

**常见追问：**

- 如果证据不足，拒答还是让模型自由发挥？依据是什么？

---

### Q16. 从「SSH 上去一脸懵」到能独立查故障 能力要上生产，你会优先保证哪三件事？ {#q16}

| 维度 | 说明 |
|------|------|
| 类型 | 生产化 |
| 难度 | 高频 |
| 考点 | 生产落地 |

**参考答法（开发向）：**

结合 从「SSH 上去一脸懵」到能独立查故障 常见风险点：在桌面上找「日志文件夹」——服务器没有桌面

**推荐三件事**：
1. **SLO 与降级**：定义 P95/P99、错误预算；检索失败/模型超时时的兜底路径；
2. **可观测**：trace_id 贯穿；记录检索命中、token、工具调用、人工介入；
3. **变更安全**：配置与 Prompt 版本化；灰度 + 自动回滚条件；回归评测门禁。

面试时用 **一个真实指标** 收尾（如 citation 准确率、empty retrieval 率）。

**常见追问：**

- 如何做成本归因到租户/业务线？
- on-call 值班最需要哪三张大盘？

---

### Q17. 用 2 分钟讲清楚 从「SSH 上去一脸懵」到能独立查故障 的技术方案（面向面试官）。 {#q17}

| 维度 | 说明 |
|------|------|
| 类型 | 综合题 |
| 难度 | 高频 |
| 考点 | 技术表述 |

**参考答法（开发向）：**

**30 秒版**：问题背景 → 核心组件 → 一条量化结果。

**2 分钟版**（可按链路讲）：在 Windows 上怎么练（WSL2 简述） → 读完本篇你能解决哪类问题？ → 命令行是什么：和图形界面怎么分工 → 终端里每一截是什么意思

每层补一句：**输入输出 + 失败怎么办**。
避免背章节标题；用「请求进来以后……」的叙事更自然。

**常见追问：**

- 如果面试官只给白板，你会先画哪张图？
- 你最自豪的一次优化是什么？

---

## 自测清单

- [ ] 能画主链路数据流（请求 → 核心服务 → 依赖 → 返回）
- [ ] 能举 1 个线上/d demo 中的真实案例
- [ ] 能说出 2 个失败模式 + 排查命令/日志字段
- [ ] 能讲清与相邻技术栈的分工（不要大包大揽）

**复习正文**：[12.linux-commands-log-tutorial.md](../12.linux-commands-log-tutorial.md)
