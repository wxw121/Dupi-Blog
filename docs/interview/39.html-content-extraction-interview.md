# 39 RAG 数据采集与解析（三）：HTML 正文抽取完全指南 · 开发面试题

> **知识来源**：[39.html-content-extraction-tutorial.md](../39.html-content-extraction-tutorial.md)（复习用，面试不考「是否读过」）  
> **题量**：18 道  
> **岗位**：后端 / AI 应用 / RAG·Agent 工程开发  
> **建议**：先口头作答，再对照「参考答法」；重点练 **设计、排障、trade-off**，不是背文档目录。

---

## 题目目录

1. [什么是 正文抽取？在 HTML 正文抽取 相关系统里它承担什么职责？](#q1)（概念题）
2. [什么是 SSRF？在 HTML 正文抽取 相关系统里它承担什么职责？](#q2)（概念题）
3. [什么是 去模板？在 HTML 正文抽取 相关系统里它承担什么职责？](#q3)（概念题）
4. [什么是 链接密度？在 HTML 正文抽取 相关系统里它承担什么职责？](#q4)（概念题）
5. [如何设计/实现「模板重复与全站检索污染」？（HTML 正文抽取）](#q5)（设计题）
6. [如何设计/实现「最小实战：BeautifulSoup」？（HTML 正文抽取）](#q6)（设计题）
7. [如何设计/实现「分块与元数据」？（HTML 正文抽取）](#q7)（设计题）
8. [如何设计/实现「用户提交 URL 的最小安全闸」？（HTML 正文抽取）](#q8)（设计题）
9. [生产环境 HTML 正文抽取 出现：1. **整页 innerText 入库** —— 检索污染，必做抽取。 2. **只删 scrip…](#q9)（排障题）
10. [Q： 里有时是文章标题怎么办？](#q10)（概念题）
11. [Q：评论区要进 RAG 吗？](#q11)（概念题）
12. [Q：和 Markdown 解析谁先？](#q12)（概念题）
13. [做 HTML 正文抽取 时，哪些能力应在当前服务内实现，哪些应下沉到专用组件或另开服务？](#q13)（架构边界）
14. [正文抽取 和 SSRF 有什么区别？在 HTML 正文抽取 里如何选型？](#q14)（对比题）
15. [场景题：现象 → 原因… —— 在 HTML 正文抽取 架构下你怎么处理？](#q15)（场景题）
16. [场景题：答案引用了菜单文字 → 导航在每个页面重复，embedding 权重高… —— 在 HTML 正文抽取 架构下你怎么处理？](#q16)（场景题）
17. [HTML 正文抽取 能力要上生产，你会优先保证哪三件事？](#q17)（生产化）
18. [用 2 分钟讲清楚 HTML 正文抽取 的技术方案（面向面试官）。](#q18)（综合题）

---

### Q1. 什么是 正文抽取？在 HTML 正文抽取 相关系统里它承担什么职责？ {#q1}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · 正文抽取 |

**参考答法（开发向）：**

**正文抽取**（Main Content Extraction）：从完整 HTML 中 **定位并输出** 承载核心信息的主体区域文本，剔除导航、装饰、广告等 **噪声**。

直觉：**从网页里只抠「文章部分」**，不是整页复印。

**工程语境（HTML 正文抽取）**：
- 解决什么：把「正文抽取」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- 正文抽取 和容易混淆的替代方案怎么选？
- 如果去掉 正文抽取 这一层，最先坏掉的是哪一环？

---

### Q2. 什么是 SSRF？在 HTML 正文抽取 相关系统里它承担什么职责？ {#q2}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · SSRF |

**参考答法（开发向）：**

**SSRF**（Server-Side Request Forgery，服务端请求伪造）：攻击者让你方服务器去请求 **内网地址**（如 ）。

直觉：**借你的爬虫之手掏内网**——用户 URL 入库必须做 **域名白名单 / IP 封锁**。

**工程语境（HTML 正文抽取）**：
- 解决什么：把「SSRF」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- SSRF 和容易混淆的替代方案怎么选？
- 如果去掉 SSRF 这一层，最先坏掉的是哪一环？

---

### Q3. 什么是 去模板？在 HTML 正文抽取 相关系统里它承担什么职责？ {#q3}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · 去模板 |

**参考答法（开发向）：**

**去模板**（Template Deduplication）：对多页 HTML 比较 DOM 公共子树，剥离相同 header/footer。

直觉：**上千页共用的门框只存一份，不要每页复制进索引**。

**工程语境（HTML 正文抽取）**：
- 解决什么：把「去模板」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- 去模板 和容易混淆的替代方案怎么选？
- 如果去掉 去模板 这一层，最先坏掉的是哪一环？

---

### Q4. 什么是 链接密度？在 HTML 正文抽取 相关系统里它承担什么职责？ {#q4}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · 链接密度 |

**参考答法（开发向）：**

**链接密度**（Link Density）：页面文字中 **锚文本字符占全文比例**；导航区往往 **链接多、说明少**。

直觉：**一行里十个蓝字链接，多半不是正文段落**。

**工程语境（HTML 正文抽取）**：
- 解决什么：把「链接密度」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- 链接密度 和容易混淆的替代方案怎么选？
- 如果去掉 链接密度 这一层，最先坏掉的是哪一环？

---

### Q5. 如何设计/实现「模板重复与全站检索污染」？（HTML 正文抽取） {#q5}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 模板重复与全站检索污染 |

**参考答法（开发向）：**

**背景要点**：**正文抽取**（Main Content Extraction）：从完整 HTML 中 **定位并输出** 承载核心信息的主体区域文本，剔除导航、装饰、广告等 **噪声**。 通俗说：**从网页里只抠「文章部分」**，不是整页复印。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q6. 如何设计/实现「最小实战：BeautifulSoup」？（HTML 正文抽取） {#q6}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 最小实战：BeautifulSoup |

**参考答法（开发向）：**

**背景要点**：| 现象 | 原因 | |------|------| | 答案引用了菜单文字 | 导航在每个页面重复，embedding 权重高 | | 检索「价格」命中页脚 | 页脚也有「价格」链接文字 | | 上下文充满 script 残留 | 未删 内容 | | 中文变菱形乱码 | 编码猜错 |

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q7. 如何设计/实现「分块与元数据」？（HTML 正文抽取） {#q7}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 分块与元数据 |

**参考答法（开发向）：**

**背景要点**：**批量抓取** 公开文档站（遵守 robots.txt 与法务）；

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q8. 如何设计/实现「用户提交 URL 的最小安全闸」？（HTML 正文抽取） {#q8}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 用户提交 URL 的最小安全闸 |

**参考答法（开发向）：**

**背景要点**：**导出** CMS 静态 HTML；

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q9. 生产环境 HTML 正文抽取 出现：1. **整页 innerText 入库** —— 检索污染，必做抽取。 2. **只删 script 不删 nav**… —— 排查思路是什么？ {#q9}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：1. **整页 innerText 入库** —— 检索污染，必做抽取。 2. **只删 script 不删 nav** —— 菜单仍重复全站。 3. **忽略编码** —— 中文库变「乱码库」。 4. **用 CSS 选择器硬编码一家站** —— 换模板就崩；监控 兜底率。 5. **抓取即入库，不缓存原文** —— 无法回归调试抽取规则。

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q10. Q： 里有时是文章标题怎么办？ {#q10}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 中等 |
| 考点 | 常见误区 |

**参考答法（开发向）：**

A: 区分 **站点 header** 与 **article 内 header**；优先在 **内部** 找标题，再删外层 header。

**常见追问：**

- 在 HTML 正文抽取 项目里你实际遇到过吗？最后怎么定的？

---

### Q11. Q：评论区要进 RAG 吗？ {#q11}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 中等 |
| 考点 | 常见误区 |

**参考答法（开发向）：**

A: 看产品——技术支持场景常 **不要**（噪声大）；社区 FAQ 可 **单独索引** 。

**常见追问：**

- 在 HTML 正文抽取 项目里你实际遇到过吗？最后怎么定的？

---

### Q12. Q：和 Markdown 解析谁先？ {#q12}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 中等 |
| 考点 | 常见误区 |

**参考答法（开发向）：**

A: 有 MD 源先 MD；只有 HTML 走本篇；PDF 走 37。

**常见追问：**

- 在 HTML 正文抽取 项目里你实际遇到过吗？最后怎么定的？

---

### Q13. 做 HTML 正文抽取 时，哪些能力应在当前服务内实现，哪些应下沉到专用组件或另开服务？ {#q13}

| 维度 | 说明 |
|------|------|
| 类型 | 架构边界 |
| 难度 | 中等 |
| 考点 | 架构分层 |

**参考答法（开发向）：**

考察 **分层与职责单一**，避免一个服务包揽 HTML 正文抽取 全链路。

**通常不在同一层硬做的**（示例）：
- ** 完整爬虫合规与反爬、浏览器指纹、大规模分布式抓取、CSS 选择器大全。

**答题要点**：
- 当前层只保留 **决策 + 编排 + 领域状态**；
- IO 密集（向量库、对象存储、消息队列）通过稳定接口调用；
- 说清楚 **同步调用 vs 异步任务** 的切分依据（延迟预算、失败重试）。

**常见追问：**

- 团队很小、没有平台组时，边界会不会变？怎么避免过度拆分？

---

### Q14. 正文抽取 和 SSRF 有什么区别？在 HTML 正文抽取 里如何选型？ {#q14}

| 维度 | 说明 |
|------|------|
| 类型 | 对比题 |
| 难度 | 中等 |
| 考点 | 对比 · 正文抽取 vs SSRF |

**参考答法（开发向）：**

| 维度 | 正文抽取 | SSRF |
|------|----------|----------|
| 定义 | 从完整 HTML 中 **定位并输出** 承载核心信息的主体区域文本，剔除导航、装饰、广告等 **噪声**。 | 攻击者让你方服务器去请求 **内网地址**（如 ）。 |
| 适用 | 见具体场景约束 | 见具体场景约束 |

**选型建议**：
- 用 **输入形态**（精确词 vs 语义改写）、**延迟预算**、**运维成本** 三张表说话；
- 给出「默认选谁 + 何时叠加谁」，必要时 Hybrid；
- 用评测集证明，而不是团队偏好。

**常见追问：**

- 能否画一张两路召回合并的时序图？
- 线上如何做 A/B？

---

### Q15. 场景题：现象 → 原因… —— 在 HTML 正文抽取 架构下你怎么处理？ {#q15}

| 维度 | 说明 |
|------|------|
| 类型 | 场景题 |
| 难度 | 中等 |
| 考点 | 业务场景 |

**参考答法（开发向）：**

**场景**：现象 → 原因

**答题要点**：
- 先复述用户目标与约束（权限、延迟、是否允许拒答）；
- 给出 **数据流**：从请求进入到返回证据/答案；
- 点出 1～2 个容易翻车的配置（top_k、filter、超时）；
- 说明如何 **验收**（命中率、引用正确率、P95）。

**常见追问：**

- 如果证据不足，拒答还是让模型自由发挥？依据是什么？

---

### Q16. 场景题：答案引用了菜单文字 → 导航在每个页面重复，embedding 权重高… —— 在 HTML 正文抽取 架构下你怎么处理？ {#q16}

| 维度 | 说明 |
|------|------|
| 类型 | 场景题 |
| 难度 | 中等 |
| 考点 | 业务场景 |

**参考答法（开发向）：**

**场景**：答案引用了菜单文字 → 导航在每个页面重复，embedding 权重高

**答题要点**：
- 先复述用户目标与约束（权限、延迟、是否允许拒答）；
- 给出 **数据流**：从请求进入到返回证据/答案；
- 点出 1～2 个容易翻车的配置（top_k、filter、超时）；
- 说明如何 **验收**（命中率、引用正确率、P95）。

**常见追问：**

- 如果证据不足，拒答还是让模型自由发挥？依据是什么？

---

### Q17. HTML 正文抽取 能力要上生产，你会优先保证哪三件事？ {#q17}

| 维度 | 说明 |
|------|------|
| 类型 | 生产化 |
| 难度 | 高频 |
| 考点 | 生产落地 |

**参考答法（开发向）：**

结合 HTML 正文抽取 常见风险点：**正文抽取**（Main Content Extraction）：从完整 HTML 中 **定位并输出** 承载核心信息的主体区域文本，剔除导航、装饰、广告等 **噪声**。 通俗说：**从网页里只抠「文章部分」**，不是整页复印。

**推荐三件事**：
1. **SLO 与降级**：定义 P95/P99、错误预算；检索失败/模型超时时的兜底路径；
2. **可观测**：trace_id 贯穿；记录检索命中、token、工具调用、人工介入；
3. **变更安全**：配置与 Prompt 版本化；灰度 + 自动回滚条件；回归评测门禁。

面试时用 **一个真实指标** 收尾（如 citation 准确率、empty retrieval 率）。

**常见追问：**

- 如何做成本归因到租户/业务线？
- on-call 值班最需要哪三张大盘？

---

### Q18. 用 2 分钟讲清楚 HTML 正文抽取 的技术方案（面向面试官）。 {#q18}

| 维度 | 说明 |
|------|------|
| 类型 | 综合题 |
| 难度 | 高频 |
| 考点 | 技术表述 |

**参考答法（开发向）：**

**30 秒版**：问题背景 → 核心组件 → 一条量化结果。

**2 分钟版**（可按链路讲）：动手路径表 → 沿用前文 → HTML 在 RAG 链路中的位置 → 三种入库来源对比

每层补一句：**输入输出 + 失败怎么办**。
避免背章节标题；用「请求进来以后……」的叙事更自然。

**常见追问：**

- 如果面试官只给白板，你会先画哪张图？
- 你最自豪的一次优化是什么？

---

## 自测清单

- [ ] 能画主链路数据流（请求 → 核心服务 → 依赖 → 返回）
- [ ] 能举 1 个线上/d demo 中的真实案例
- [ ] 能说出 2 个失败模式 + 排查命令/日志字段
- [ ] 能讲清与相邻技术栈的分工（不要大包大揽）

**复习正文**：[39.html-content-extraction-tutorial.md](../39.html-content-extraction-tutorial.md)
