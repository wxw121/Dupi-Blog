# 251 AI Agent 工程（三十八）：构建研究 Agent · 开发面试题 · **热点**

> **知识来源**：[251.build-research-agent-tutorial.md](../251.build-research-agent-tutorial.md)（复习用，面试不考「是否读过」）  
> **题量**：19 道  
> **岗位**：后端 / AI 应用 / RAG·Agent 工程开发  
> **建议**：先口头作答，再对照「参考答法」；重点练 **设计、排障、trade-off**，不是背文档目录。

---

## 题目目录

1. [你怎么设计一个研究型 Agent？](#q1)（综合题）
2. [什么是 综合实战？在 构建研究 Agent 相关系统里它承担什么职责？](#q2)（概念题）
3. [什么是 交叉验证？在 构建研究 Agent 相关系统里它承担什么职责？](#q3)（概念题）
4. [什么是 synthesize？在 构建研究 Agent 相关系统里它承担什么职责？](#q4)（概念题）
5. [请说明在 构建研究 Agent 场景下如何设计研究 Agent 的多步搜索策略。](#q5)（实现题）
6. [请说明在 构建研究 Agent 场景下如何实现信息来源的交叉验证。](#q6)（实现题）
7. [在 构建研究 Agent 相关开发中，构建"搜索→阅读→提取→综合"管道——你会如何实现或验证？](#q7)（实现题）
8. [在 构建研究 Agent 相关开发中，处理信息冲突和不确定性——你会如何实现或验证？](#q8)（实现题）
9. [如何设计/实现「先错后对：单次检索写报告」？（构建研究 Agent）](#q9)（设计题）
10. [如何设计/实现「先错对对：单次检索写报告」？（构建研究 Agent）](#q10)（设计题）
11. [如何设计/实现「工程化版本」？（构建研究 Agent）](#q11)（设计题）
12. [如何设计/实现「常见失败模式」？（构建研究 Agent）](#q12)（设计题）
13. [生产环境 构建研究 Agent 出现：**演示什么：** 本节最小可运行切片。**环境：** 见文首。**预期：** 控制台打印 dem…](#q13)（排障题）
14. [生产环境 构建研究 Agent 出现：class ResearchPhase(str, Enum): PLANNING = "plann…](#q14)（排障题）
15. [生产环境 构建研究 Agent 出现：@dataclass class SearchPlan: """研究计划。""" original…](#q15)（排障题）
16. [生产环境 构建研究 Agent 出现：@dataclass class Source: """信息来源。""" source_id: s…](#q16)（排障题）
17. [综合实战 和 交叉验证 有什么区别？在 构建研究 Agent 里如何选型？](#q17)（对比题）
18. [构建研究 Agent 能力要上生产，你会优先保证哪三件事？](#q18)（生产化）
19. [用 2 分钟讲清楚 构建研究 Agent 的技术方案（面向面试官）。](#q19)（综合题）

---

### Q1. 你怎么设计一个研究型 Agent？ {#q1}

| 维度 | 说明 |
|------|------|
| 类型 | 综合题 |
| 难度 | 高频 |
| 考点 | 系统设计 / 表述 |

**参考答法（开发向）：**

我设计了五阶段研究管道：规划、搜索、阅读、验证、综合。 规划阶段用 LLM 把大问题分解为子查询。 搜索阶段多轮执行，覆盖不同角度。 阅读阶段提取关键声明。 验证阶段交叉对比，标记冲突。 综合阶段生成带引用和置信度的报告。 用自适应策略控制深度：信息够了就停，不够就追问。 超过阈值（如 0.7）就停止。 如果无法解决，标记为"存在争议"，降低置信度。 结果缓存 24 小时。单次预算上限 $1。

**常见追问：**

- 怎么判断"信息够了"？ 覆盖度评分：综合置信度 × 0.6 + 来源多样性 × 0.4。
- 信息冲突怎么处理？ 加权投票：高可信来源权重更大。
- 成本怎么控制？ 三档深度（Quick/Standard/Deep）。

---

### Q2. 什么是 综合实战？在 构建研究 Agent 相关系统里它承担什么职责？ {#q2}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · 综合实战 |

**参考答法（开发向）：**

**综合实战**（End-to-end Project）：在同一代码库上交付可运行 Agent 切片。

直觉：从零件到能跑的原型车。

**工程语境（构建研究 Agent）**：
- 解决什么：把「综合实战」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- 综合实战 和容易混淆的替代方案怎么选？
- 如果去掉 综合实战 这一层，最先坏掉的是哪一环？

---

### Q3. 什么是 交叉验证？在 构建研究 Agent 相关系统里它承担什么职责？ {#q3}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · 交叉验证 |

**参考答法（开发向）：**

**交叉验证**（Cross Verification）：多来源信息对照，标记冲突与置信度。

直觉：多来源信息对照。

**工程语境（构建研究 Agent）**：
- 解决什么：把「交叉验证」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- 交叉验证 和容易混淆的替代方案怎么选？
- 如果去掉 交叉验证 这一层，最先坏掉的是哪一环？

---

### Q4. 什么是 synthesize？在 构建研究 Agent 相关系统里它承担什么职责？ {#q4}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · synthesize |

**参考答法（开发向）：**

**synthesize**（综合工具）：把多条要点合并成带结构的报告。

直觉：把多条要点合并成带结构的报告。

**工程语境（构建研究 Agent）**：
- 解决什么：把「synthesize」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- synthesize 和容易混淆的替代方案怎么选？
- 如果去掉 synthesize 这一层，最先坏掉的是哪一环？

---

### Q5. 请说明在 构建研究 Agent 场景下如何设计研究 Agent 的多步搜索策略。 {#q5}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：设计研究 Agent 的多步搜索策略

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q6. 请说明在 构建研究 Agent 场景下如何实现信息来源的交叉验证。 {#q6}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：实现信息来源的交叉验证

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q7. 在 构建研究 Agent 相关开发中，构建"搜索→阅读→提取→综合"管道——你会如何实现或验证？ {#q7}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：构建"搜索→阅读→提取→综合"管道

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q8. 在 构建研究 Agent 相关开发中，处理信息冲突和不确定性——你会如何实现或验证？ {#q8}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：处理信息冲突和不确定性

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q9. 如何设计/实现「先错后对：单次检索写报告」？（构建研究 Agent） {#q9}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 先错后对：单次检索写报告 |

**参考答法（开发向）：**

**背景要点**：!构建研究 Agent核心思想 对照上图：研究 Agent 走「分解→多轮搜索→交叉验证→综合报告」——不是单次检索总结。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q10. 如何设计/实现「先错对对：单次检索写报告」？（构建研究 Agent） {#q10}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 先错对对：单次检索写报告 |

**参考答法（开发向）：**

**背景要点**：读下图时，先看「简单 RAG 做研究」的局限——单次检索、信息片面、无法交叉验证；下文用多步研究管道补齐。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q11. 如何设计/实现「工程化版本」？（构建研究 Agent） {#q11}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 工程化版本 |

**参考答法（开发向）：**

**背景要点**：```text 阶段 1：规划（Plan） 输入：用户的研究问题 输出：搜索计划（子问题列表、搜索策略） 决策：需要搜几轮？用什么关键词？

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q12. 如何设计/实现「常见失败模式」？（构建研究 Agent） {#q12}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 常见失败模式 |

**参考答法（开发向）：**

**背景要点**：阶段 2：搜索（Search） 输入：子查询 输出：候选文档列表 工具：web_search, internal_search, paper_search

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q13. 生产环境 构建研究 Agent 出现：**演示什么：** 本节最小可运行切片。**环境：** 见文首。**预期：** 控制台打印 demo 输出。… —— 排查思路是什么？ {#q13}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：**演示什么：** 本节最小可运行切片。**环境：** 见文首。**预期：** 控制台打印 demo 输出。

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q14. 生产环境 构建研究 Agent 出现：class ResearchPhase(str, Enum): PLANNING = "planning" SEARCH… —— 排查思路是什么？ {#q14}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：class ResearchPhase(str, Enum): PLANNING = "planning" SEARCHING = "searching" READING = "reading" VERIFYING = "verifying" SYNTHESIZING = "synthesizing" DONE = "done"

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q15. 生产环境 构建研究 Agent 出现：@dataclass class SearchPlan: """研究计划。""" original_question: … —— 排查思路是什么？ {#q15}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：@dataclass class SearchPlan: """研究计划。""" original_question: str sub_queries: list[str] max_sources: int = 10 strategy: str = "breadth_first" # breadth_first | depth_first

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q16. 生产环境 构建研究 Agent 出现：@dataclass class Source: """信息来源。""" source_id: str title: s… —— 排查思路是什么？ {#q16}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：@dataclass class Source: """信息来源。""" source_id: str title: str url: str content: str relevance: float = 0.0 # 0-1 credibility: float = 0.5 # 0-1

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q17. 综合实战 和 交叉验证 有什么区别？在 构建研究 Agent 里如何选型？ {#q17}

| 维度 | 说明 |
|------|------|
| 类型 | 对比题 |
| 难度 | 中等 |
| 考点 | 对比 · 综合实战 vs 交叉验证 |

**参考答法（开发向）：**

| 维度 | 综合实战 | 交叉验证 |
|------|----------|----------|
| 定义 | 在同一代码库上交付可运行 Agent 切片。 | 多来源信息对照，标记冲突与置信度。 |
| 适用 | 见具体场景约束 | 见具体场景约束 |

**选型建议**：
- 用 **输入形态**（精确词 vs 语义改写）、**延迟预算**、**运维成本** 三张表说话；
- 给出「默认选谁 + 何时叠加谁」，必要时 Hybrid；
- 用评测集证明，而不是团队偏好。

**常见追问：**

- 能否画一张两路召回合并的时序图？
- 线上如何做 A/B？

---

### Q18. 构建研究 Agent 能力要上生产，你会优先保证哪三件事？ {#q18}

| 维度 | 说明 |
|------|------|
| 类型 | 生产化 |
| 难度 | 高频 |
| 考点 | 生产落地 |

**参考答法（开发向）：**

结合 构建研究 Agent 常见风险点：!构建研究 Agent核心思想 对照上图：研究 Agent 走「分解→多轮搜索→交叉验证→综合报告」——不是单次检索总结。

**推荐三件事**：
1. **SLO 与降级**：定义 P95/P99、错误预算；检索失败/模型超时时的兜底路径；
2. **可观测**：trace_id 贯穿；记录检索命中、token、工具调用、人工介入；
3. **变更安全**：配置与 Prompt 版本化；灰度 + 自动回滚条件；回归评测门禁。

面试时用 **一个真实指标** 收尾（如 citation 准确率、empty retrieval 率）。

**常见追问：**

- 如何做成本归因到租户/业务线？
- on-call 值班最需要哪三张大盘？

---

### Q19. 用 2 分钟讲清楚 构建研究 Agent 的技术方案（面向面试官）。 {#q19}

| 维度 | 说明 |
|------|------|
| 类型 | 综合题 |
| 难度 | 高频 |
| 考点 | 技术表述 |

**参考答法（开发向）：**

**30 秒版**：问题背景 → 核心组件 → 一条量化结果。

**2 分钟版**（可按链路讲）：它解决什么问题 → 单次搜索的局限 → 研究 Agent vs 普通 RAG → 核心概念：研究 Agent 的工作流

每层补一句：**输入输出 + 失败怎么办**。
避免背章节标题；用「请求进来以后……」的叙事更自然。

**常见追问：**

- 如果面试官只给白板，你会先画哪张图？
- 你最自豪的一次优化是什么？

---

## 自测清单

- [ ] 能画主链路数据流（请求 → 核心服务 → 依赖 → 返回）
- [ ] 能举 1 个线上/d demo 中的真实案例
- [ ] 能说出 2 个失败模式 + 排查命令/日志字段
- [ ] 能讲清与相邻技术栈的分工（不要大包大揽）

**复习正文**：[251.build-research-agent-tutorial.md](../251.build-research-agent-tutorial.md)
