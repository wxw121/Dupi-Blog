# 62 RAG 数据采集与解析（十七）：结构感知分块（标题层级）完全指南 · 开发面试题

> **知识来源**：[62.structure-aware-chunking-tutorial.md](../62.structure-aware-chunking-tutorial.md)（复习用，面试不考「是否读过」）  
> **题量**：15 道  
> **岗位**：后端 / AI 应用 / RAG·Agent 工程开发  
> **建议**：先口头作答，再对照「参考答法」；重点练 **设计、排障、trade-off**，不是背文档目录。

---

## 题目目录

1. [什么是 Structure-aware Chunking？在 结构感知分块（标题层级） 相关系统里它承担什么职责？](#q1)（概念题）
2. [什么是 Heading Hierarchy？在 结构感知分块（标题层级） 相关系统里它承担什么职责？](#q2)（概念题）
3. [什么是 Section？在 结构感知分块（标题层级） 相关系统里它承担什么职责？](#q3)（概念题）
4. [什么是 Semantic Chunking？在 结构感知分块（标题层级） 相关系统里它承担什么职责？](#q4)（概念题）
5. [如何设计/实现「结构感知分块在链路中的位置」？（结构感知分块（标题层级））](#q5)（设计题）
6. [如何设计/实现「与「语义分块」的区别」？（结构感知分块（标题层级））](#q6)（设计题）
7. [如何设计/实现「与固定长度分块对比」？（结构感知分块（标题层级））](#q7)（设计题）
8. [如何设计/实现「最小实战：Python 按 H2 分块」？（结构感知分块（标题层级））](#q8)（设计题）
9. [生产环境 结构感知分块（标题层级） 出现：[ ] 与固定 500 字对比 **3 条** bad case… —— 排查思路是什么？](#q9)（排障题）
10. [做 结构感知分块（标题层级） 时，哪些能力应在当前服务内实现，哪些应下沉到专用组件或另开服务？](#q10)（架构边界）
11. [Structure-aware Chunking 和 Heading Hierarchy 有什么区别？在 结构感知分块（标题层级） 里如…](#q11)（对比题）
12. [场景题：条目 → 关系… —— 在 结构感知分块（标题层级） 架构下你怎么处理？](#q12)（场景题）
13. [场景题：**38** MD 解析 → AST/事件是 **实现手段**… —— 在 结构感知分块（标题层级） 架构下你怎么处理？](#q13)（场景题）
14. [结构感知分块（标题层级） 能力要上生产，你会优先保证哪三件事？](#q14)（生产化）
15. [用 2 分钟讲清楚 结构感知分块（标题层级） 的技术方案（面向面试官）。](#q15)（综合题）

---

### Q1. 什么是 Structure-aware Chunking？在 结构感知分块（标题层级） 相关系统里它承担什么职责？ {#q1}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Structure-aware Chunking |

**参考答法（开发向）：**

**Structure-aware Chunking**（结构感知分块）：利用文档已有 **逻辑结构**（标题、条款、样式、AST 节点类型）决定 chunk 边界，而非仅按字符数或句号。

直觉：**按作者写的章节目录切，不按字数盲切**。

**工程语境（结构感知分块（标题层级））**：
- 解决什么：把「Structure-aware Chunking」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Structure-aware Chunking 和容易混淆的替代方案怎么选？
- 如果去掉 Structure-aware Chunking 这一层，最先坏掉的是哪一环？

---

### Q2. 什么是 Heading Hierarchy？在 结构感知分块（标题层级） 相关系统里它承担什么职责？ {#q2}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Heading Hierarchy |

**参考答法（开发向）：**

**Heading Hierarchy**（标题层级）：H1 文题 → H2 章 → H3 节……形成的 **树状大纲**。

直觉：**# 多少级，就是第几层标题**。

**工程语境（结构感知分块（标题层级））**：
- 解决什么：把「Heading Hierarchy」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Heading Hierarchy 和容易混淆的替代方案怎么选？
- 如果去掉 Heading Hierarchy 这一层，最先坏掉的是哪一环？

---

### Q3. 什么是 Section？在 结构感知分块（标题层级） 相关系统里它承担什么职责？ {#q3}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Section |

**参考答法（开发向）：**

**Section**（节）：标题层级下 **归属同一 H2（或 H3）** 的正文聚合单元，常作为 **一个 chunk 或 chunk 序列**。

直觉：**「第三章 报销流程」整节是一张或一组卡片**。

**工程语境（结构感知分块（标题层级））**：
- 解决什么：把「Section」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Section 和容易混淆的替代方案怎么选？
- 如果去掉 Section 这一层，最先坏掉的是哪一环？

---

### Q4. 什么是 Semantic Chunking？在 结构感知分块（标题层级） 相关系统里它承担什么职责？ {#q4}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Semantic Chunking |

**参考答法（开发向）：**

**Semantic Chunking**（语义分块）：用 Embedding 相似度 **自动找** 话题边界——无需标题，但 **算力贵、边界难解释**。

**工程语境（结构感知分块（标题层级））**：
- 解决什么：把「Semantic Chunking」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Semantic Chunking 和容易混淆的替代方案怎么选？
- 如果去掉 Semantic Chunking 这一层，最先坏掉的是哪一环？

---

### Q5. 如何设计/实现「结构感知分块在链路中的位置」？（结构感知分块（标题层级）） {#q5}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 结构感知分块在链路中的位置 |

**参考答法（开发向）：**

**背景要点**：**Structure-aware Chunking**（结构感知分块）：利用文档已有 **逻辑结构**（标题、条款、样式、AST 节点类型）决定 chunk 边界，而非仅按字符数或句号。 通俗说：**按作者写的章节目录切，不按字数盲切**。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q6. 如何设计/实现「与「语义分块」的区别」？（结构感知分块（标题层级）） {#q6}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 与「语义分块」的区别 |

**参考答法（开发向）：**

**背景要点**：**Heading Hierarchy**（标题层级）：H1 文题 → H2 章 → H3 节……形成的 **树状大纲**。 通俗说：**# 多少级，就是第几层标题**。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q7. 如何设计/实现「与固定长度分块对比」？（结构感知分块（标题层级）） {#q7}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 与固定长度分块对比 |

**参考答法（开发向）：**

**背景要点**：| 输入形态 | 结构来源 | 本篇适用度 | |----------|----------|------------| | Markdown | 标题 | ★★★★★ | | DOCX | Heading 样式 | ★★★★（40 篇） | | HTML | h1/h2 标签 | ★★★（39 篇） | | PDF | 常需布局模型 | ★★（37 篇） | | 纯 TXT | 无 | 降级 59+61 |

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q8. 如何设计/实现「最小实战：Python 按 H2 分块」？（结构感知分块（标题层级）） {#q8}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 最小实战：Python 按 H2 分块 |

**参考答法（开发向）：**

**背景要点**：**Section**（节）：标题层级下 **归属同一 H2（或 H3）** 的正文聚合单元，常作为 **一个 chunk 或 chunk 序列**。 通俗说：**「第三章 报销流程」整节是一张或一组卡片**。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q9. 生产环境 结构感知分块（标题层级） 出现：[ ] 与固定 500 字对比 **3 条** bad case… —— 排查思路是什么？ {#q9}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：[ ] 与固定 500 字对比 **3 条** bad case

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q10. 做 结构感知分块（标题层级） 时，哪些能力应在当前服务内实现，哪些应下沉到专用组件或另开服务？ {#q10}

| 维度 | 说明 |
|------|------|
| 类型 | 架构边界 |
| 难度 | 中等 |
| 考点 | 架构分层 |

**参考答法（开发向）：**

考察 **分层与职责单一**，避免一个服务包揽 结构感知分块（标题层级） 全链路。

**通常不在同一层硬做的**（示例）：
- ** Markdown AST 全节点遍历（路线图 **70**）、LayoutLM PDF 标题检测、GraphRAG 社区发现、多文件站点全局 TOC 合并。

**答题要点**：
- 当前层只保留 **决策 + 编排 + 领域状态**；
- IO 密集（向量库、对象存储、消息队列）通过稳定接口调用；
- 说清楚 **同步调用 vs 异步任务** 的切分依据（延迟预算、失败重试）。

**常见追问：**

- 团队很小、没有平台组时，边界会不会变？怎么避免过度拆分？

---

### Q11. Structure-aware Chunking 和 Heading Hierarchy 有什么区别？在 结构感知分块（标题层级） 里如何选型？ {#q11}

| 维度 | 说明 |
|------|------|
| 类型 | 对比题 |
| 难度 | 中等 |
| 考点 | 对比 · Structure-aware Chunking vs Heading Hierarchy |

**参考答法（开发向）：**

| 维度 | Structure-aware Chunking | Heading Hierarchy |
|------|----------|----------|
| 定义 | 利用文档已有 **逻辑结构**（标题、条款、样式、AST 节点类型）决定 chunk 边界，而非仅按字符数或句号。 | H1 文题 → H2 章 → H3 节……形成的 **树状大纲**。 |
| 适用 | 见具体场景约束 | 见具体场景约束 |

**选型建议**：
- 用 **输入形态**（精确词 vs 语义改写）、**延迟预算**、**运维成本** 三张表说话；
- 给出「默认选谁 + 何时叠加谁」，必要时 Hybrid；
- 用评测集证明，而不是团队偏好。

**常见追问：**

- 能否画一张两路召回合并的时序图？
- 线上如何做 A/B？

---

### Q12. 场景题：条目 → 关系… —— 在 结构感知分块（标题层级） 架构下你怎么处理？ {#q12}

| 维度 | 说明 |
|------|------|
| 类型 | 场景题 |
| 难度 | 中等 |
| 考点 | 业务场景 |

**参考答法（开发向）：**

**场景**：条目 → 关系

**答题要点**：
- 先复述用户目标与约束（权限、延迟、是否允许拒答）；
- 给出 **数据流**：从请求进入到返回证据/答案；
- 点出 1～2 个容易翻车的配置（top_k、filter、超时）；
- 说明如何 **验收**（命中率、引用正确率、P95）。

**常见追问：**

- 如果证据不足，拒答还是让模型自由发挥？依据是什么？

---

### Q13. 场景题：**38** MD 解析 → AST/事件是 **实现手段**… —— 在 结构感知分块（标题层级） 架构下你怎么处理？ {#q13}

| 维度 | 说明 |
|------|------|
| 类型 | 场景题 |
| 难度 | 中等 |
| 考点 | 业务场景 |

**参考答法（开发向）：**

**场景**：**38** MD 解析 → AST/事件是 **实现手段**

**答题要点**：
- 先复述用户目标与约束（权限、延迟、是否允许拒答）；
- 给出 **数据流**：从请求进入到返回证据/答案；
- 点出 1～2 个容易翻车的配置（top_k、filter、超时）；
- 说明如何 **验收**（命中率、引用正确率、P95）。

**常见追问：**

- 如果证据不足，拒答还是让模型自由发挥？依据是什么？

---

### Q14. 结构感知分块（标题层级） 能力要上生产，你会优先保证哪三件事？ {#q14}

| 维度 | 说明 |
|------|------|
| 类型 | 生产化 |
| 难度 | 高频 |
| 考点 | 生产落地 |

**参考答法（开发向）：**

结合 结构感知分块（标题层级） 常见风险点：**Structure-aware Chunking**（结构感知分块）：利用文档已有 **逻辑结构**（标题、条款、样式、AST 节点类型）决定 chunk 边界，而非仅按字符数或句号。 通俗说：**按作者写的章节目录切，不按字数盲切**。

**推荐三件事**：
1. **SLO 与降级**：定义 P95/P99、错误预算；检索失败/模型超时时的兜底路径；
2. **可观测**：trace_id 贯穿；记录检索命中、token、工具调用、人工介入；
3. **变更安全**：配置与 Prompt 版本化；灰度 + 自动回滚条件；回归评测门禁。

面试时用 **一个真实指标** 收尾（如 citation 准确率、empty retrieval 率）。

**常见追问：**

- 如何做成本归因到租户/业务线？
- on-call 值班最需要哪三张大盘？

---

### Q15. 用 2 分钟讲清楚 结构感知分块（标题层级） 的技术方案（面向面试官）。 {#q15}

| 维度 | 说明 |
|------|------|
| 类型 | 综合题 |
| 难度 | 高频 |
| 考点 | 技术表述 |

**参考答法（开发向）：**

**30 秒版**：问题背景 → 核心组件 → 一条量化结果。

**2 分钟版**（可按链路讲）：动手路径表（建议按顺序） → 与路线图前后条的关系 → 结构感知分块在链路中的位置 → 与「语义分块」的区别

每层补一句：**输入输出 + 失败怎么办**。
避免背章节标题；用「请求进来以后……」的叙事更自然。

**常见追问：**

- 如果面试官只给白板，你会先画哪张图？
- 你最自豪的一次优化是什么？

---

## 自测清单

- [ ] 能画主链路数据流（请求 → 核心服务 → 依赖 → 返回）
- [ ] 能举 1 个线上/d demo 中的真实案例
- [ ] 能说出 2 个失败模式 + 排查命令/日志字段
- [ ] 能讲清与相邻技术栈的分工（不要大包大揽）

**复习正文**：[62.structure-aware-chunking-tutorial.md](../62.structure-aware-chunking-tutorial.md)
