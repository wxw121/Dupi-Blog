# 21 NLP / IR / LLM 基础（五）：Word2Vec 与静态词向量完全指南 · 开发面试题

> **知识来源**：[21.word2vec-static-embeddings-tutorial.md](../21.word2vec-static-embeddings-tutorial.md)（复习用，面试不考「是否读过」）  
> **题量**：15 道  
> **岗位**：后端 / AI 应用 / RAG·Agent 工程开发  
> **建议**：先口头作答，再对照「参考答法」；重点练 **设计、排障、trade-off**，不是背文档目录。

---

## 题目目录

1. [什么是 B 轨第五篇？在 Word2Vec 与静态词向量 相关系统里它承担什么职责？](#q1)（概念题）
2. [什么是 词向量？在 Word2Vec 与静态词向量 相关系统里它承担什么职责？](#q2)（概念题）
3. [什么是 静态词向量？在 Word2Vec 与静态词向量 相关系统里它承担什么职责？](#q3)（概念题）
4. [什么是 分布式假设？在 Word2Vec 与静态词向量 相关系统里它承担什么职责？](#q4)（概念题）
5. [如何设计/实现「Word2Vec 的两种架构：CBOW 与 Skip-gram」？（Word2Vec 与静态词向量）](#q5)（设计题）
6. [如何设计/实现「最小代码：看一眼预训练向量」？（Word2Vec 与静态词向量）](#q6)（设计题）
7. [生产环境 Word2Vec 与静态词向量 出现：**陷阱 1：用 Word2Vec 平均向量当生产 RAG 唯一检索** 句向量 + H…](#q7)（排障题）
8. [生产环境 Word2Vec 与静态词向量 出现：**陷阱 2：以为向量近就能自动替换词** 法务、医药领域 **近义不等于可替换**。……](#q8)（排障题）
9. [生产环境 Word2Vec 与静态词向量 出现：**陷阱 3：中文不统一分词就训 / 就用** 与 第 17 篇 索引、查询 **同一分…](#q9)（排障题）
10. [生产环境 Word2Vec 与静态词向量 出现：**陷阱 4：忽略 OOV** 新品代号、内部缩写无向量 → 召回空洞；稀疏一路或关键词…](#q10)（排障题）
11. [B 轨第五篇 和 词向量 有什么区别？在 Word2Vec 与静态词向量 里如何选型？](#q11)（对比题）
12. [场景题：篇章 → 回答的问题… —— 在 Word2Vec 与静态词向量 架构下你怎么处理？](#q12)（场景题）
13. [场景题：17 分词 → term 怎么切… —— 在 Word2Vec 与静态词向量 架构下你怎么处理？](#q13)（场景题）
14. [Word2Vec 与静态词向量 能力要上生产，你会优先保证哪三件事？](#q14)（生产化）
15. [用 2 分钟讲清楚 Word2Vec 与静态词向量 的技术方案（面向面试官）。](#q15)（综合题）

---

### Q1. 什么是 B 轨第五篇？在 Word2Vec 与静态词向量 相关系统里它承担什么职责？ {#q1}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · B 轨第五篇 |

**参考答法（开发向）：**

**B 轨第五篇**（路线图第 28 条）：概念为主，说明 **静态词向量** 是什么、怎么学出来、与稀疏检索如何分工；代码仅保留加载预训练向量的最小示例。前置 分词、倒排索引。

**工程语境（Word2Vec 与静态词向量）**：
- 解决什么：把「B 轨第五篇」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- B 轨第五篇 和容易混淆的替代方案怎么选？
- 如果去掉 B 轨第五篇 这一层，最先坏掉的是哪一环？

---

### Q2. 什么是 词向量？在 Word2Vec 与静态词向量 相关系统里它承担什么职责？ {#q2}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · 词向量 |

**参考答法（开发向）：**

**词向量**（word embedding，词嵌入）：把词映射到 **固定长度** 的实数向量（如 300 维），使得语义相近的词向量 **距离更近**。

直觉：给每个词发一张 **坐标卡**，意思相近的词住在邻近街区——不再各占一个互不相干的稀疏维度。

**工程语境（Word2Vec 与静态词向量）**：
- 解决什么：把「词向量」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- 词向量 和容易混淆的替代方案怎么选？
- 如果去掉 词向量 这一层，最先坏掉的是哪一环？

---

### Q3. 什么是 静态词向量？在 Word2Vec 与静态词向量 相关系统里它承担什么职责？ {#q3}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · 静态词向量 |

**参考答法（开发向）：**

**静态词向量**（static embedding）：无论出现在哪句话里，「银行」永远对应 **同一张** 坐标卡。

直觉：**一词一向量**——后面会看到，多义词会吃亏；BERT 一类 **动态向量** 才随上下文变。

**工程语境（Word2Vec 与静态词向量）**：
- 解决什么：把「静态词向量」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- 静态词向量 和容易混淆的替代方案怎么选？
- 如果去掉 静态词向量 这一层，最先坏掉的是哪一环？

---

### Q4. 什么是 分布式假设？在 Word2Vec 与静态词向量 相关系统里它承担什么职责？ {#q4}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · 分布式假设 |

**参考答法（开发向）：**

**分布式假设**（distributional hypothesis）：词的意思由它 **经常出现的上下文** 决定。

直觉：**物以类聚，词以伴生**——经常和「驾驶、轮胎、油耗」一起出现的词，多半和「汽车」一类。

**工程语境（Word2Vec 与静态词向量）**：
- 解决什么：把「分布式假设」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- 分布式假设 和容易混淆的替代方案怎么选？
- 如果去掉 分布式假设 这一层，最先坏掉的是哪一环？

---

### Q5. 如何设计/实现「Word2Vec 的两种架构：CBOW 与 Skip-gram」？（Word2Vec 与静态词向量） {#q5}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · Word2Vec 的两种架构：CBOW  |

**参考答法（开发向）：**

**背景要点**：典型场景：制度库 chunk 写「**机动车**年检流程」，用户问「**汽车**年检怎么办」。BM25 若没命中共同词项，分数接近零——而人眼知道这是在问同一件事。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q6. 如何设计/实现「最小代码：看一眼预训练向量」？（Word2Vec 与静态词向量） {#q6}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 最小代码：看一眼预训练向量 |

**参考答法（开发向）：**

**背景要点**：**词向量**（word embedding，词嵌入）：把词映射到 **固定长度** 的实数向量（如 300 维），使得语义相近的词向量 **距离更近**。 通俗说：给每个词发一张 **坐标卡**，意思相近的词住在邻近街区——不再各占一个互不相干的稀疏维度。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q7. 生产环境 Word2Vec 与静态词向量 出现：**陷阱 1：用 Word2Vec 平均向量当生产 RAG 唯一检索** 句向量 + Hybrid + 重排是常态；词平… —— 排查思路是什么？ {#q7}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：**陷阱 1：用 Word2Vec 平均向量当生产 RAG 唯一检索** 句向量 + Hybrid + 重排是常态；词平均丢语序与句意。

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q8. 生产环境 Word2Vec 与静态词向量 出现：**陷阱 2：以为向量近就能自动替换词** 法务、医药领域 **近义不等于可替换**。… —— 排查思路是什么？ {#q8}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：**陷阱 2：以为向量近就能自动替换词** 法务、医药领域 **近义不等于可替换**。

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q9. 生产环境 Word2Vec 与静态词向量 出现：**陷阱 3：中文不统一分词就训 / 就用** 与 第 17 篇 索引、查询 **同一分词器** 原则类似；训与用、索引… —— 排查思路是什么？ {#q9}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：**陷阱 3：中文不统一分词就训 / 就用** 与 第 17 篇 索引、查询 **同一分词器** 原则类似；训与用、索引与查询要 **一致**。

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q10. 生产环境 Word2Vec 与静态词向量 出现：**陷阱 4：忽略 OOV** 新品代号、内部缩写无向量 → 召回空洞；稀疏一路或关键词仍要保留。… —— 排查思路是什么？ {#q10}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：**陷阱 4：忽略 OOV** 新品代号、内部缩写无向量 → 召回空洞；稀疏一路或关键词仍要保留。

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q11. B 轨第五篇 和 词向量 有什么区别？在 Word2Vec 与静态词向量 里如何选型？ {#q11}

| 维度 | 说明 |
|------|------|
| 类型 | 对比题 |
| 难度 | 中等 |
| 考点 | 对比 · B 轨第五篇 vs 词向量 |

**参考答法（开发向）：**

| 维度 | B 轨第五篇 | 词向量 |
|------|----------|----------|
| 定义 | 概念为主，说明 **静态词向量** 是什么、怎么学出来、与稀疏检索如何分工；代码仅保留加载预训练向量的最小示例。前置 分词、倒排索引。 | 把词映射到 **固定长度** 的实数向量（如 300 维），使得语义相近的词向量 **距离更近**。 |
| 适用 | 见具体场景约束 | 见具体场景约束 |

**选型建议**：
- 用 **输入形态**（精确词 vs 语义改写）、**延迟预算**、**运维成本** 三张表说话；
- 给出「默认选谁 + 何时叠加谁」，必要时 Hybrid；
- 用评测集证明，而不是团队偏好。

**常见追问：**

- 能否画一张两路召回合并的时序图？
- 线上如何做 A/B？

---

### Q12. 场景题：篇章 → 回答的问题… —— 在 Word2Vec 与静态词向量 架构下你怎么处理？ {#q12}

| 维度 | 说明 |
|------|------|
| 类型 | 场景题 |
| 难度 | 中等 |
| 考点 | 业务场景 |

**参考答法（开发向）：**

**场景**：篇章 → 回答的问题

**答题要点**：
- 先复述用户目标与约束（权限、延迟、是否允许拒答）；
- 给出 **数据流**：从请求进入到返回证据/答案；
- 点出 1～2 个容易翻车的配置（top_k、filter、超时）；
- 说明如何 **验收**（命中率、引用正确率、P95）。

**常见追问：**

- 如果证据不足，拒答还是让模型自由发挥？依据是什么？

---

### Q13. 场景题：17 分词 → term 怎么切… —— 在 Word2Vec 与静态词向量 架构下你怎么处理？ {#q13}

| 维度 | 说明 |
|------|------|
| 类型 | 场景题 |
| 难度 | 中等 |
| 考点 | 业务场景 |

**参考答法（开发向）：**

**场景**：17 分词 → term 怎么切

**答题要点**：
- 先复述用户目标与约束（权限、延迟、是否允许拒答）；
- 给出 **数据流**：从请求进入到返回证据/答案；
- 点出 1～2 个容易翻车的配置（top_k、filter、超时）；
- 说明如何 **验收**（命中率、引用正确率、P95）。

**常见追问：**

- 如果证据不足，拒答还是让模型自由发挥？依据是什么？

---

### Q14. Word2Vec 与静态词向量 能力要上生产，你会优先保证哪三件事？ {#q14}

| 维度 | 说明 |
|------|------|
| 类型 | 生产化 |
| 难度 | 高频 |
| 考点 | 生产落地 |

**参考答法（开发向）：**

结合 Word2Vec 与静态词向量 常见风险点：典型场景：制度库 chunk 写「**机动车**年检流程」，用户问「**汽车**年检怎么办」。BM25 若没命中共同词项，分数接近零——而人眼知道这是在问同一件事。

**推荐三件事**：
1. **SLO 与降级**：定义 P95/P99、错误预算；检索失败/模型超时时的兜底路径；
2. **可观测**：trace_id 贯穿；记录检索命中、token、工具调用、人工介入；
3. **变更安全**：配置与 Prompt 版本化；灰度 + 自动回滚条件；回归评测门禁。

面试时用 **一个真实指标** 收尾（如 citation 准确率、empty retrieval 率）。

**常见追问：**

- 如何做成本归因到租户/业务线？
- on-call 值班最需要哪三张大盘？

---

### Q15. 用 2 分钟讲清楚 Word2Vec 与静态词向量 的技术方案（面向面试官）。 {#q15}

| 维度 | 说明 |
|------|------|
| 类型 | 综合题 |
| 难度 | 高频 |
| 考点 | 技术表述 |

**参考答法（开发向）：**

**30 秒版**：问题背景 → 核心组件 → 一条量化结果。

**2 分钟版**（可按链路讲）：和前几篇的分工 → 从稀疏到稠密：向量长什么样 → 回顾：TF-IDF 向量是「稀疏」的 → 稠密词向量：维数少、几乎无零

每层补一句：**输入输出 + 失败怎么办**。
避免背章节标题；用「请求进来以后……」的叙事更自然。

**常见追问：**

- 如果面试官只给白板，你会先画哪张图？
- 你最自豪的一次优化是什么？

---

## 自测清单

- [ ] 能画主链路数据流（请求 → 核心服务 → 依赖 → 返回）
- [ ] 能举 1 个线上/d demo 中的真实案例
- [ ] 能说出 2 个失败模式 + 排查命令/日志字段
- [ ] 能讲清与相邻技术栈的分工（不要大包大揽）

**复习正文**：[21.word2vec-static-embeddings-tutorial.md](../21.word2vec-static-embeddings-tutorial.md)
