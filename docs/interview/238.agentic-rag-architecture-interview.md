# 238 AI Agent 工程（二十五）：Agentic RAG 生产架构 · 开发面试题 · **超高频**

> **知识来源**：[238.agentic-rag-architecture-tutorial.md](../238.agentic-rag-architecture-tutorial.md)（复习用，面试不考「是否读过」）  
> **题量**：21 道  
> **岗位**：后端 / AI 应用 / RAG·Agent 工程开发  
> **建议**：先口头作答，再对照「参考答法」；重点练 **设计、排障、trade-off**，不是背文档目录。

---

## 题目目录

1. [什么是 Agentic RAG？在 Agentic RAG 生产架构 相关系统里它承担什么职责？](#q1)（概念题）
2. [什么是 Evidence？在 Agentic RAG 生产架构 相关系统里它承担什么职责？](#q2)（概念题）
3. [什么是 Sufficiency Check？在 Agentic RAG 生产架构 相关系统里它承担什么职责？](#q3)（概念题）
4. [什么是 Query Planning？在 Agentic RAG 生产架构 相关系统里它承担什么职责？](#q4)（概念题）
5. [请说明在 Agentic RAG 生产架构 场景下如何区分 Agentic RAG 与普通 RAG 的决策循环。](#q5)（实现题）
6. [请说明在 Agentic RAG 生产架构 场景下如何说出四个核心组件与两项基础设施。](#q6)（实现题）
7. [在 Agentic RAG 生产架构 相关开发中，为 239–242 各专题建立全局地图——你会如何实现或验证？](#q7)（实现题）
8. [在 Agentic RAG 生产架构 相关开发中，判断何时不必上 Agentic RAG——你会如何实现或验证？](#q8)（实现题）
9. [如何设计/实现「工程化版本」？（Agentic RAG 生产架构）](#q9)（设计题）
10. [如何设计/实现「局限一：一次检索无法覆盖多子问题」？（Agentic RAG 生产架构）](#q10)（设计题）
11. [如何设计/实现「局限二：检索结果不够时无法追问」？（Agentic RAG 生产架构）](#q11)（设计题）
12. [如何设计/实现「怎么做：四个核心组件的工程实现」？（Agentic RAG 生产架构）](#q12)（设计题）
13. [生产环境 Agentic RAG 生产架构 出现：理解了正确做法之后，让我们看看实际开发中最常犯的错误。这些失败模式每一个都会导致答案质…](#q13)（排障题）
14. [我建议你在阅读这些失败模式时，不要只是"记住不要这样做"，而是思考"为什么开发者会犯这个错误"。大多数失败模式的根源不是"开发者不知道正…](#q14)（概念题）
15. [生产环境 Agentic RAG 生产架构 出现：这是最常见的错误。Planner 没有看到已有证据，每轮都生成语义相同但措辞不同的查询…](#q15)（排障题）
16. [生产环境 Agentic RAG 生产架构 出现：这个错误的根源是 Planner 没有"上下文感知"。它不知道"已经查了什么、找到了什…](#q16)（排障题）
17. [Agentic RAG 和 Evidence 有什么区别？在 Agentic RAG 生产架构 里如何选型？](#q17)（对比题）
18. [场景题：术语 → 通俗说… —— 在 Agentic RAG 生产架构 架构下你怎么处理？](#q18)（场景题）
19. [场景题：Query Planning → 拆子问题并选数据源，不是改写一句话… —— 在 Agentic RAG 生产架构 架构下你怎么…](#q19)（场景题）
20. [Agentic RAG 生产架构 能力要上生产，你会优先保证哪三件事？](#q20)（生产化）
21. [用 2 分钟讲清楚 Agentic RAG 生产架构 的技术方案（面向面试官）。](#q21)（综合题）

---

### Q1. 什么是 Agentic RAG？在 Agentic RAG 生产架构 相关系统里它承担什么职责？ {#q1}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Agentic RAG |

**参考答法（开发向）：**

**Agentic RAG**（自主检索增强）：根据中间证据自主决定查什么、查几次、何时停。

直觉：像侦探——线索不够就继续查，够了再下结论。

**工程语境（Agentic RAG 生产架构）**：
- 解决什么：把「Agentic RAG」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Agentic RAG 和容易混淆的替代方案怎么选？
- 如果去掉 Agentic RAG 这一层，最先坏掉的是哪一环？

---

### Q2. 什么是 Evidence？在 Agentic RAG 生产架构 相关系统里它承担什么职责？ {#q2}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Evidence |

**参考答法（开发向）：**

**Evidence**（证据）：检索得到的原文片段，生成与引用必须能指回它。

直觉：卷宗里的复印件，不能凭空虚构。

**工程语境（Agentic RAG 生产架构）**：
- 解决什么：把「Evidence」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Evidence 和容易混淆的替代方案怎么选？
- 如果去掉 Evidence 这一层，最先坏掉的是哪一环？

---

### Q3. 什么是 Sufficiency Check？在 Agentic RAG 生产架构 相关系统里它承担什么职责？ {#q3}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Sufficiency Check |

**参考答法（开发向）：**

**Sufficiency Check**（充分性检查）：判断当前证据是否够回答用户问题。

直觉：对比类问题必须两侧都查到，不能只查到一边就喊「够了」。

**工程语境（Agentic RAG 生产架构）**：
- 解决什么：把「Sufficiency Check」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Sufficiency Check 和容易混淆的替代方案怎么选？
- 如果去掉 Sufficiency Check 这一层，最先坏掉的是哪一环？

---

### Q4. 什么是 Query Planning？在 Agentic RAG 生产架构 相关系统里它承担什么职责？ {#q4}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Query Planning |

**参考答法（开发向）：**

**Query Planning**（查询规划）：把复杂问题拆成可检索子问题并选数据源

直觉：旅行规划师列行程，不是改写一句话。

**工程语境（Agentic RAG 生产架构）**：
- 解决什么：把「Query Planning」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Query Planning 和容易混淆的替代方案怎么选？
- 如果去掉 Query Planning 这一层，最先坏掉的是哪一环？

---

### Q5. 请说明在 Agentic RAG 生产架构 场景下如何区分 Agentic RAG 与普通 RAG 的决策循环。 {#q5}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：区分 Agentic RAG 与普通 RAG 的决策循环

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q6. 请说明在 Agentic RAG 生产架构 场景下如何说出四个核心组件与两项基础设施。 {#q6}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：说出四个核心组件与两项基础设施

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q7. 在 Agentic RAG 生产架构 相关开发中，为 239–242 各专题建立全局地图——你会如何实现或验证？ {#q7}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：为 239–242 各专题建立全局地图

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q8. 在 Agentic RAG 生产架构 相关开发中，判断何时不必上 Agentic RAG——你会如何实现或验证？ {#q8}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：判断何时不必上 Agentic RAG

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q9. 如何设计/实现「工程化版本」？（Agentic RAG 生产架构） {#q9}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 工程化版本 |

**参考答法（开发向）：**

**背景要点**：**演示什么：** 本节最小可运行切片。**环境：** 见文首。**预期：** 控制台打印 demo 输出。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q10. 如何设计/实现「局限一：一次检索无法覆盖多子问题」？（Agentic RAG 生产架构） {#q10}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 局限一：一次检索无法覆盖多子问题 |

**参考答法（开发向）：**

**背景要点**：> 本篇为概念总览；最小可运行切片见文末 与 239+ lab。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q11. 如何设计/实现「局限二：检索结果不够时无法追问」？（Agentic RAG 生产架构） {#q11}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 局限二：检索结果不够时无法追问 |

**参考答法（开发向）：**

**背景要点**：读下图时，先看左右对比：**左**普通 RAG 一次检索易缺证据；**右** Agentic RAG 在充分性为 False 时继续检索，两侧齐全后再生成。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q12. 如何设计/实现「怎么做：四个核心组件的工程实现」？（Agentic RAG 生产架构） {#q12}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 怎么做：四个核心组件的工程实现 |

**参考答法（开发向）：**

**背景要点**：假设你是一个企业知识库的开发者。某天，产品经理问你一个问题："帮我对比一下我们高级报表套餐和基础套餐的功能差异，特别是数据源数量和导出格式方面。"

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q13. 生产环境 Agentic RAG 生产架构 出现：理解了正确做法之后，让我们看看实际开发中最常犯的错误。这些失败模式每一个都会导致答案质量下降或资源浪费，了解它们能帮你在… —— 排查思路是什么？ {#q13}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：理解了正确做法之后，让我们看看实际开发中最常犯的错误。这些失败模式每一个都会导致答案质量下降或资源浪费，了解它们能帮你在代码审查时快速识别风险。我把它们按"发现难度"排序——越往后越隐蔽，也越危险。理解这些失败模式的价值不仅在于避免它们，更在于理解"为什么正确做法是那样的"——很多设计决策正是为了避免这些失败而做出的。

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q14. 我建议你在阅读这些失败模式时，不要只是"记住不要这样做"，而是思考"为什么开发者会犯这个错误"。大多数失败模式的根源不是"开发者不知道正确做法"，而是"正确做法… {#q14}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 中等 |
| 考点 | FAQ |

**参考答法（开发向）：**

**问题本质**：我建议你在阅读这些失败模式时，不要只是"记住不要这样做"，而是思考"为什么开发者会犯这个错误"。大多数失败模式的根源不是"开发者不知道正确做法"，而是"正确做法在短期内看起来更复杂、更慢、更贵"。比如，"不保存 query_id"看起来节省了存储空间，但出问题时你就无法追溯。"不做引用验证"看起来减少了延迟，但幻觉就溜进了答案。理解这些权衡能帮你在面对"要不要省这一步"的决策时做出正确选择。

**工程视角**：
- 先区分是 **配置/数据/代码逻辑** 哪类问题；
- 给出可落地的判断标准（阈值、日志、评测指标），不要只给「看情况」。

**常见追问：**

- 有没有对应的监控或告警规则？

---

### Q15. 生产环境 Agentic RAG 生产架构 出现：这是最常见的错误。Planner 没有看到已有证据，每轮都生成语义相同但措辞不同的查询。比如第一轮查"高级套餐功能"，第… —— 排查思路是什么？ {#q15}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：这是最常见的错误。Planner 没有看到已有证据，每轮都生成语义相同但措辞不同的查询。比如第一轮查"高级套餐功能"，第二轮查"高级套餐有哪些功能"，第三轮查"高级套餐的功能列表"——三次查询返回几乎相同的结果，浪费了检索预算却没有新信息。

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q16. 生产环境 Agentic RAG 生产架构 出现：这个错误的根源是 Planner 没有"上下文感知"。它不知道"已经查了什么、找到了什么"，所以只能根据原始问题重新生成… —— 排查思路是什么？ {#q16}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：这个错误的根源是 Planner 没有"上下文感知"。它不知道"已经查了什么、找到了什么"，所以只能根据原始问题重新生成查询。而原始问题没变，生成的查询自然也不会变。这就像一个人在图书馆里找书，每次去书架都问同一个问题，当然每次都得到同样的答案。

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q17. Agentic RAG 和 Evidence 有什么区别？在 Agentic RAG 生产架构 里如何选型？ {#q17}

| 维度 | 说明 |
|------|------|
| 类型 | 对比题 |
| 难度 | 中等 |
| 考点 | 对比 · Agentic RAG vs Evidence |

**参考答法（开发向）：**

| 维度 | Agentic RAG | Evidence |
|------|----------|----------|
| 定义 | 根据中间证据自主决定查什么、查几次、何时停。 | 检索得到的原文片段，生成与引用必须能指回它。 |
| 适用 | 见具体场景约束 | 见具体场景约束 |

**选型建议**：
- 用 **输入形态**（精确词 vs 语义改写）、**延迟预算**、**运维成本** 三张表说话；
- 给出「默认选谁 + 何时叠加谁」，必要时 Hybrid；
- 用评测集证明，而不是团队偏好。

**常见追问：**

- 能否画一张两路召回合并的时序图？
- 线上如何做 A/B？

---

### Q18. 场景题：术语 → 通俗说… —— 在 Agentic RAG 生产架构 架构下你怎么处理？ {#q18}

| 维度 | 说明 |
|------|------|
| 类型 | 场景题 |
| 难度 | 中等 |
| 考点 | 业务场景 |

**参考答法（开发向）：**

**场景**：术语 → 通俗说

**答题要点**：
- 先复述用户目标与约束（权限、延迟、是否允许拒答）；
- 给出 **数据流**：从请求进入到返回证据/答案；
- 点出 1～2 个容易翻车的配置（top_k、filter、超时）；
- 说明如何 **验收**（命中率、引用正确率、P95）。

**常见追问：**

- 如果证据不足，拒答还是让模型自由发挥？依据是什么？

---

### Q19. 场景题：Query Planning → 拆子问题并选数据源，不是改写一句话… —— 在 Agentic RAG 生产架构 架构下你怎么处理？ {#q19}

| 维度 | 说明 |
|------|------|
| 类型 | 场景题 |
| 难度 | 中等 |
| 考点 | 业务场景 |

**参考答法（开发向）：**

**场景**：Query Planning → 拆子问题并选数据源，不是改写一句话

**答题要点**：
- 先复述用户目标与约束（权限、延迟、是否允许拒答）；
- 给出 **数据流**：从请求进入到返回证据/答案；
- 点出 1～2 个容易翻车的配置（top_k、filter、超时）；
- 说明如何 **验收**（命中率、引用正确率、P95）。

**常见追问：**

- 如果证据不足，拒答还是让模型自由发挥？依据是什么？

---

### Q20. Agentic RAG 生产架构 能力要上生产，你会优先保证哪三件事？ {#q20}

| 维度 | 说明 |
|------|------|
| 类型 | 生产化 |
| 难度 | 高频 |
| 考点 | 生产落地 |

**参考答法（开发向）：**

结合 Agentic RAG 生产架构 常见风险点：**演示什么：** 本节最小可运行切片。**环境：** 见文首。**预期：** 控制台打印 demo 输出。

**推荐三件事**：
1. **SLO 与降级**：定义 P95/P99、错误预算；检索失败/模型超时时的兜底路径；
2. **可观测**：trace_id 贯穿；记录检索命中、token、工具调用、人工介入；
3. **变更安全**：配置与 Prompt 版本化；灰度 + 自动回滚条件；回归评测门禁。

面试时用 **一个真实指标** 收尾（如 citation 准确率、empty retrieval 率）。

**常见追问：**

- 如何做成本归因到租户/业务线？
- on-call 值班最需要哪三张大盘？

---

### Q21. 用 2 分钟讲清楚 Agentic RAG 生产架构 的技术方案（面向面试官）。 {#q21}

| 维度 | 说明 |
|------|------|
| 类型 | 综合题 |
| 难度 | 高频 |
| 考点 | 技术表述 |

**参考答法（开发向）：**

**30 秒版**：问题背景 → 核心组件 → 一条量化结果。

**2 分钟版**（可按链路讲）：最小示例 → 工程化版本 → 是什么：Agentic RAG 到底在做什么 → 从一个真实场景说起

每层补一句：**输入输出 + 失败怎么办**。
避免背章节标题；用「请求进来以后……」的叙事更自然。

**常见追问：**

- 如果面试官只给白板，你会先画哪张图？
- 你最自豪的一次优化是什么？

---

## 自测清单

- [ ] 能画主链路数据流（请求 → 核心服务 → 依赖 → 返回）
- [ ] 能举 1 个线上/d demo 中的真实案例
- [ ] 能说出 2 个失败模式 + 排查命令/日志字段
- [ ] 能讲清与相邻技术栈的分工（不要大包大揽）

**复习正文**：[238.agentic-rag-architecture-tutorial.md](../238.agentic-rag-architecture-tutorial.md)
