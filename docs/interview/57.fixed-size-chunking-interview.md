# 57 企业 RAG 分块（一）：固定长度分块完全指南 · 开发面试题 · **热点**

> **知识来源**：[57.fixed-size-chunking-tutorial.md](../57.fixed-size-chunking-tutorial.md)（复习用，面试不考「是否读过」）  
> **题量**：18 道  
> **岗位**：后端 / AI 应用 / RAG·Agent 工程开发  
> **建议**：先口头作答，再对照「参考答法」；重点练 **设计、排障、trade-off**，不是背文档目录。

---

## 题目目录

1. [什么是 Chunking？在 固定长度分块 相关系统里它承担什么职责？](#q1)（概念题）
2. [什么是 Fixed-size chunking？在 固定长度分块 相关系统里它承担什么职责？](#q2)（概念题）
3. [什么是 Chunk？在 固定长度分块 相关系统里它承担什么职责？](#q3)（概念题）
4. [什么是 Overlap？在 固定长度分块 相关系统里它承担什么职责？](#q4)（概念题）
5. [如何设计/实现「固定长度分块是什么」？（固定长度分块）](#q5)（设计题）
6. [如何设计/实现「tiktoken 最小用法」？（固定长度分块）](#q6)（设计题）
7. [如何设计/实现「可运行 Python：从零实现切分器」？（固定长度分块）](#q7)（设计题）
8. [如何设计/实现「对法预览（不本篇实现）」？（固定长度分块）](#q8)（设计题）
9. [生产环境 固定长度分块 出现：stride = chunk_size − chunk_overlap… —— 排查思路是什么？](#q9)（排障题）
10. [生产环境 固定长度分块 出现：chunks ≈ ceil(total_len / stride)（字符模式近似）… —— 排查思路是什么…](#q10)（排障题）
11. [生产环境 固定长度分块 出现：token 生产：embed max 定 chunk_size 上限… —— 排查思路是什么？](#q11)（排障题）
12. [生产环境 固定长度分块 出现：overlap 建议：size 的 10%～20%… —— 排查思路是什么？](#q12)（排障题）
13. [做 固定长度分块 时，哪些能力应在当前服务内实现，哪些应下沉到专用组件或另开服务？](#q13)（架构边界）
14. [Chunking 和 Fixed-size chunking 有什么区别？在 固定长度分块 里如何选型？](#q14)（对比题）
15. [场景题：条目 → 关系… —— 在 固定长度分块 架构下你怎么处理？](#q15)（场景题）
16. [场景题：51 chunk_id → 每个 chunk 稳定 ID… —— 在 固定长度分块 架构下你怎么处理？](#q16)（场景题）
17. [固定长度分块 能力要上生产，你会优先保证哪三件事？](#q17)（生产化）
18. [用 2 分钟讲清楚 固定长度分块 的技术方案（面向面试官）。](#q18)（综合题）

---

### Q1. 什么是 Chunking？在 固定长度分块 相关系统里它承担什么职责？ {#q1}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Chunking |

**参考答法（开发向）：**

**Chunking**（分块）：把长文本切成 **大小适中、可独立检索** 的片段。

**工程语境（固定长度分块）**：
- 解决什么：把「Chunking」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Chunking 和容易混淆的替代方案怎么选？
- 如果去掉 Chunking 这一层，最先坏掉的是哪一环？

---

### Q2. 什么是 Fixed-size chunking？在 固定长度分块 相关系统里它承担什么职责？ {#q2}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Fixed-size chunking |

**参考答法（开发向）：**

**Fixed-size chunking**（固定长度分块）：按预设 **字符数或 token 数**（加可选 overlap）滑动窗口切分， **不感知** 段落/标题边界。

直觉：**每隔 N 个字切一刀**，最简单粗暴。

**工程语境（固定长度分块）**：
- 解决什么：把「Fixed-size chunking」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Fixed-size chunking 和容易混淆的替代方案怎么选？
- 如果去掉 Fixed-size chunking 这一层，最先坏掉的是哪一环？

---

### Q3. 什么是 Chunk？在 固定长度分块 相关系统里它承担什么职责？ {#q3}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Chunk |

**参考答法（开发向）：**

**Chunk**（块 / 片段）：入库的最小检索单元，通常含 + 。

直觉：**知识库里的「一条笔记」**。

**工程语境（固定长度分块）**：
- 解决什么：把「Chunk」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Chunk 和容易混淆的替代方案怎么选？
- 如果去掉 Chunk 这一层，最先坏掉的是哪一环？

---

### Q4. 什么是 Overlap？在 固定长度分块 相关系统里它承担什么职责？ {#q4}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Overlap |

**参考答法（开发向）：**

**Overlap**（重叠）：相邻 chunk 之间 **故意重复** 的一段字符/token，减少边界信息被切断。

直觉：**切刀留一点回头重叠**，避免答案刚好卡在缝上。

**工程语境（固定长度分块）**：
- 解决什么：把「Overlap」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Overlap 和容易混淆的替代方案怎么选？
- 如果去掉 Overlap 这一层，最先坏掉的是哪一环？

---

### Q5. 如何设计/实现「固定长度分块是什么」？（固定长度分块） {#q5}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 固定长度分块是什么 |

**参考答法（开发向）：**

**背景要点**：**Embedding 模型** 有 **最大输入长度**（如 512/8192 token），超长会被 **截断** 或报错；

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q6. 如何设计/实现「tiktoken 最小用法」？（固定长度分块） {#q6}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · tiktoken 最小用法 |

**参考答法（开发向）：**

**背景要点**：**检索** 命中整本手册时，塞进 LLM 的 context **爆窗口**；

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q7. 如何设计/实现「可运行 Python：从零实现切分器」？（固定长度分块） {#q7}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 可运行 Python：从零实现切分器 |

**参考答法（开发向）：**

**背景要点**：读下图：长文像一根香肠，固定长度就是 **按固定宽度切片**；overlap 是 **相邻片重叠**。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q8. 如何设计/实现「对法预览（不本篇实现）」？（固定长度分块） {#q8}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 对法预览（不本篇实现） |

**参考答法（开发向）：**

**背景要点**：下面这张图说明固定长度分块的基本直觉。读图时重点看：它不理解语义，只是按照字符数或 token 数把长文本切成一段一段。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q9. 生产环境 固定长度分块 出现：stride = chunk_size − chunk_overlap… —— 排查思路是什么？ {#q9}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：stride = chunk_size − chunk_overlap

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q10. 生产环境 固定长度分块 出现：chunks ≈ ceil(total_len / stride)（字符模式近似）… —— 排查思路是什么？ {#q10}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：chunks ≈ ceil(total_len / stride)（字符模式近似）

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q11. 生产环境 固定长度分块 出现：token 生产：embed max 定 chunk_size 上限… —— 排查思路是什么？ {#q11}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：token 生产：embed max 定 chunk_size 上限

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q12. 生产环境 固定长度分块 出现：overlap 建议：size 的 10%～20%… —— 排查思路是什么？ {#q12}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：overlap 建议：size 的 10%～20%

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q13. 做 固定长度分块 时，哪些能力应在当前服务内实现，哪些应下沉到专用组件或另开服务？ {#q13}

| 维度 | 说明 |
|------|------|
| 类型 | 架构边界 |
| 难度 | 中等 |
| 考点 | 架构分层 |

**参考答法（开发向）：**

考察 **分层与职责单一**，避免一个服务包揽 固定长度分块 全链路。

**通常不在同一层硬做的**（示例）：
- ** 语义分块、LLM 自动切章、多向量 hierarchical index 实现（路线图 **74**）、具体向量库 API。

**答题要点**：
- 当前层只保留 **决策 + 编排 + 领域状态**；
- IO 密集（向量库、对象存储、消息队列）通过稳定接口调用；
- 说清楚 **同步调用 vs 异步任务** 的切分依据（延迟预算、失败重试）。

**常见追问：**

- 团队很小、没有平台组时，边界会不会变？怎么避免过度拆分？

---

### Q14. Chunking 和 Fixed-size chunking 有什么区别？在 固定长度分块 里如何选型？ {#q14}

| 维度 | 说明 |
|------|------|
| 类型 | 对比题 |
| 难度 | 中等 |
| 考点 | 对比 · Chunking vs Fixed-size chunking |

**参考答法（开发向）：**

| 维度 | Chunking | Fixed-size chunking |
|------|----------|----------|
| 定义 | 把长文本切成 **大小适中、可独立检索** 的片段。 | 按预设 **字符数或 token 数**（加可选 overlap）滑动窗口切分， **不感知** 段落/标题边界。 |
| 适用 | 见具体场景约束 | 见具体场景约束 |

**选型建议**：
- 用 **输入形态**（精确词 vs 语义改写）、**延迟预算**、**运维成本** 三张表说话；
- 给出「默认选谁 + 何时叠加谁」，必要时 Hybrid；
- 用评测集证明，而不是团队偏好。

**常见追问：**

- 能否画一张两路召回合并的时序图？
- 线上如何做 A/B？

---

### Q15. 场景题：条目 → 关系… —— 在 固定长度分块 架构下你怎么处理？ {#q15}

| 维度 | 说明 |
|------|------|
| 类型 | 场景题 |
| 难度 | 中等 |
| 考点 | 业务场景 |

**参考答法（开发向）：**

**场景**：条目 → 关系

**答题要点**：
- 先复述用户目标与约束（权限、延迟、是否允许拒答）；
- 给出 **数据流**：从请求进入到返回证据/答案；
- 点出 1～2 个容易翻车的配置（top_k、filter、超时）；
- 说明如何 **验收**（命中率、引用正确率、P95）。

**常见追问：**

- 如果证据不足，拒答还是让模型自由发挥？依据是什么？

---

### Q16. 场景题：51 chunk_id → 每个 chunk 稳定 ID… —— 在 固定长度分块 架构下你怎么处理？ {#q16}

| 维度 | 说明 |
|------|------|
| 类型 | 场景题 |
| 难度 | 中等 |
| 考点 | 业务场景 |

**参考答法（开发向）：**

**场景**：51 chunk_id → 每个 chunk 稳定 ID

**答题要点**：
- 先复述用户目标与约束（权限、延迟、是否允许拒答）；
- 给出 **数据流**：从请求进入到返回证据/答案；
- 点出 1～2 个容易翻车的配置（top_k、filter、超时）；
- 说明如何 **验收**（命中率、引用正确率、P95）。

**常见追问：**

- 如果证据不足，拒答还是让模型自由发挥？依据是什么？

---

### Q17. 固定长度分块 能力要上生产，你会优先保证哪三件事？ {#q17}

| 维度 | 说明 |
|------|------|
| 类型 | 生产化 |
| 难度 | 高频 |
| 考点 | 生产落地 |

**参考答法（开发向）：**

结合 固定长度分块 常见风险点：**Embedding 模型** 有 **最大输入长度**（如 512/8192 token），超长会被 **截断** 或报错；

**推荐三件事**：
1. **SLO 与降级**：定义 P95/P99、错误预算；检索失败/模型超时时的兜底路径；
2. **可观测**：trace_id 贯穿；记录检索命中、token、工具调用、人工介入；
3. **变更安全**：配置与 Prompt 版本化；灰度 + 自动回滚条件；回归评测门禁。

面试时用 **一个真实指标** 收尾（如 citation 准确率、empty retrieval 率）。

**常见追问：**

- 如何做成本归因到租户/业务线？
- on-call 值班最需要哪三张大盘？

---

### Q18. 用 2 分钟讲清楚 固定长度分块 的技术方案（面向面试官）。 {#q18}

| 维度 | 说明 |
|------|------|
| 类型 | 综合题 |
| 难度 | 高频 |
| 考点 | 技术表述 |

**参考答法（开发向）：**

**30 秒版**：问题背景 → 核心组件 → 一条量化结果。

**2 分钟版**（可按链路讲）：动手路径表 → 与路线图关系 → 固定长度分块是什么 → 优点与代价

每层补一句：**输入输出 + 失败怎么办**。
避免背章节标题；用「请求进来以后……」的叙事更自然。

**常见追问：**

- 如果面试官只给白板，你会先画哪张图？
- 你最自豪的一次优化是什么？

---

## 自测清单

- [ ] 能画主链路数据流（请求 → 核心服务 → 依赖 → 返回）
- [ ] 能举 1 个线上/d demo 中的真实案例
- [ ] 能说出 2 个失败模式 + 排查命令/日志字段
- [ ] 能讲清与相邻技术栈的分工（不要大包大揽）

**复习正文**：[57.fixed-size-chunking-tutorial.md](../57.fixed-size-chunking-tutorial.md)
