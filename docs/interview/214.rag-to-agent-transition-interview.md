# 214 AI Agent 工程（一）：从 RAG 走向 Agent · 开发面试题 · **热点**

> **知识来源**：[214.rag-to-agent-transition-tutorial.md](../214.rag-to-agent-transition-tutorial.md)（复习用，面试不考「是否读过」）  
> **题量**：17 道  
> **岗位**：后端 / AI 应用 / RAG·Agent 工程开发  
> **建议**：先口头作答，再对照「参考答法」；重点练 **设计、排障、trade-off**，不是背文档目录。

---

## 题目目录

1. [RAG 和 Agent 有什么区别？](#q1)（综合题）
2. [什么是 RAG？在 从 RAG 走向 Agent 相关系统里它承担什么职责？](#q2)（概念题）
3. [什么是 Agent？在 从 RAG 走向 Agent 相关系统里它承担什么职责？](#q3)（概念题）
4. [什么是 工具？在 从 RAG 走向 Agent 相关系统里它承担什么职责？](#q4)（概念题）
5. [在 从 RAG 走向 Agent 相关开发中，说清楚 RAG 和 Agent 的本质区别（不是"有没有工具"）——你会如何实现或验证？](#q5)（实现题）
6. [在 从 RAG 走向 Agent 相关开发中，判断一个需求该用 RAG 还是 Agent——你会如何实现或验证？](#q6)（实现题）
7. [在 从 RAG 走向 Agent 相关开发中，在现有 RAG 系统上增加 Agent 能力（最小改动路径）——你会如何实现或验证？](#q7)（实现题）
8. [请说明在 从 RAG 走向 Agent 场景下如何理解 Agent 的三个核心组件：感知、决策、执行。](#q8)（实现题）
9. [如何设计/实现「最小 Agent 循环」？（从 RAG 走向 Agent）](#q9)（设计题）
10. [如何设计/实现「工程化版本」？（从 RAG 走向 Agent）](#q10)（设计题）
11. [如何设计/实现「常见失败模式」？（从 RAG 走向 Agent）](#q11)（设计题）
12. [如何设计/实现「生产环境注意事项」？（从 RAG 走向 Agent）](#q12)（设计题）
13. [生产环境 从 RAG 走向 Agent 出现：A：分层处理。 简单问题（80%）→ RAG 快速路径（< 1s） 复杂问题（20%）→ …](#q13)（排障题）
14. [生产环境 从 RAG 走向 Agent 出现：A：三层保障。 1. 确定性工具：计算、查询用确定性代码 2. LLM 只做决策：不让 L…](#q14)（排障题）
15. [RAG 和 Agent 有什么区别？在 从 RAG 走向 Agent 里如何选型？](#q15)（对比题）
16. [从 RAG 走向 Agent 能力要上生产，你会优先保证哪三件事？](#q16)（生产化）
17. [用 2 分钟讲清楚 从 RAG 走向 Agent 的技术方案（面向面试官）。](#q17)（综合题）

---

### Q1. RAG 和 Agent 有什么区别？ {#q1}

| 维度 | 说明 |
|------|------|
| 类型 | 综合题 |
| 难度 | 高频 |
| 考点 | 系统设计 / 表述 |

**参考答法（开发向）：**

RAG 是"一问一答"：检索文档，生成回答，一次完成。 Agent 是"多步执行"：感知→决策→行动循环，直到任务完成。 核心区别是：RAG 无状态、被动响应；Agent 有状态、主动规划。 RAG 是 Agent 的一个工具，不是替代关系。 简单问答用 RAG，固定流程用 Workflow。 超过 max_steps 强制停止。 危险操作必须人工批准。 不要一步到位，每步充分验证。

**常见追问：**

- 什么时候用 Agent？ 需要多步操作、条件分支、调用外部系统时用 Agent。
- 怎么防止 Agent 失控？ 三道防线：步数限制、工具权限、人工确认。
- 怎么从 RAG 迁移到 Agent？ 渐进式：先加路由层，再加单工具，再扩展。

---

### Q2. 什么是 RAG？在 从 RAG 走向 Agent 相关系统里它承担什么职责？ {#q2}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · RAG |

**参考答法（开发向）：**

**RAG**（Retrieval-Augmented Generation）：检索增强生成，先查资料再让模型回答。

直觉：像开卷考试——允许翻笔记再答题。

**工程语境（从 RAG 走向 Agent）**：
- 解决什么：把「RAG」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- RAG 和容易混淆的替代方案怎么选？
- 如果去掉 RAG 这一层，最先坏掉的是哪一环？

---

### Q3. 什么是 Agent？在 从 RAG 走向 Agent 相关系统里它承担什么职责？ {#q3}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · Agent |

**参考答法（开发向）：**

**Agent**（智能体）：能根据目标自主多步决策并调用工具的系统。

直觉：像办事员——你给目标，它自己拆步骤、查资料、动手办。

**工程语境（从 RAG 走向 Agent）**：
- 解决什么：把「Agent」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- Agent 和容易混淆的替代方案怎么选？
- 如果去掉 Agent 这一层，最先坏掉的是哪一环？

---

### Q4. 什么是 工具？在 从 RAG 走向 Agent 相关系统里它承担什么职责？ {#q4}

| 维度 | 说明 |
|------|------|
| 类型 | 概念题 |
| 难度 | 基础 |
| 考点 | 核心概念 · 工具 |

**参考答法（开发向）：**

**工具**（Tool）：Agent 可调用的外部能力（搜索、计算、写库等）。

直觉：Agent 的「手」，用来碰真实世界。

**工程语境（从 RAG 走向 Agent）**：
- 解决什么：把「工具」讲清楚时，要落到 **输入/输出、放在链路哪一层、失败时怎么办**；
- 常见误用：只会背定义，说不出和相邻组件的分工；
- 加分项：举一个线上或 demo 中的真实调用点（接口、配置项、监控指标）。

**常见追问：**

- 工具 和容易混淆的替代方案怎么选？
- 如果去掉 工具 这一层，最先坏掉的是哪一环？

---

### Q5. 在 从 RAG 走向 Agent 相关开发中，说清楚 RAG 和 Agent 的本质区别（不是"有没有工具"）——你会如何实现或验证？ {#q5}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：说清楚 RAG 和 Agent 的本质区别（不是"有没有工具"）

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q6. 在 从 RAG 走向 Agent 相关开发中，判断一个需求该用 RAG 还是 Agent——你会如何实现或验证？ {#q6}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：判断一个需求该用 RAG 还是 Agent

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q7. 在 从 RAG 走向 Agent 相关开发中，在现有 RAG 系统上增加 Agent 能力（最小改动路径）——你会如何实现或验证？ {#q7}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：在现有 RAG 系统上增加 Agent 能力（最小改动路径）

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q8. 请说明在 从 RAG 走向 Agent 场景下如何理解 Agent 的三个核心组件：感知、决策、执行。 {#q8}

| 维度 | 说明 |
|------|------|
| 类型 | 实现题 |
| 难度 | 中等 |
| 考点 | 工程能力 |

**参考答法（开发向）：**

**考察点**：理解 Agent 的三个核心组件：感知、决策、执行

**建议回答结构**：
1. **接口/数据**：关键入参、出参、状态字段；
2. **流程**：主路径 + 异常路径（超时、空结果、权限拒绝）；
3. **代码层落点**：放在哪一层（API / Service / Worker / SDK）；
4. **验证**：单测 + 集成测 + 一条可观测指标（日志字段或 metric）。

尽量用「我们服务里 X 模块负责 Y」的方式讲，避免空泛形容词。

**常见追问：**

- 如果 QPS 翻倍，你会先改哪一段？
- 有哪些必须写进 Code Review checklist 的点？

---

### Q9. 如何设计/实现「最小 Agent 循环」？（从 RAG 走向 Agent） {#q9}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 最小 Agent 循环 |

**参考答法（开发向）：**

**背景要点**：读下图时，先看「从 RAG 走向 Agent核心思想」建立直觉。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q10. 如何设计/实现「工程化版本」？（从 RAG 走向 Agent） {#q10}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 工程化版本 |

**参考答法（开发向）：**

**背景要点**：!从 RAG 走向 Agent核心思想 对照上图：Level 0 纯 RAG 到 Level 2 最小 Agent 循环——关键跃迁是「能观察环境并选工具」，不是多检索几次。

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q11. 如何设计/实现「常见失败模式」？（从 RAG 走向 Agent） {#q11}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 常见失败模式 |

**参考答法（开发向）：**

**背景要点**：```text 1. 感知（Perception） 从环境获取信息 - 用户输入 - 工具返回结果 - 系统状态变化

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q12. 如何设计/实现「生产环境注意事项」？（从 RAG 走向 Agent） {#q12}

| 维度 | 说明 |
|------|------|
| 类型 | 设计题 |
| 难度 | 中等 |
| 考点 | 工程设计 · 生产环境注意事项 |

**参考答法（开发向）：**

**背景要点**：2. 决策（Decision） 根据当前状态选择下一步 - LLM 推理 - 规则引擎 - 历史经验

**设计回答框架**：
1. **目标与约束**：延迟、准确率、成本、团队规模；
2. **组件图**：列出 3～5 个模块及数据流（可手绘）；
3. **关键接口**：同步 API / 消息 / 批处理任务各负责什么；
4. **失败与降级**：超时、空检索、工具失败时的默认策略；
5. **评测**：离线指标 + 线上 shadow / 金丝雀。

**常见追问：**

- MVP 版本可以砍掉哪些部分？
- 10x 流量时最先改什么？

---

### Q13. 生产环境 从 RAG 走向 Agent 出现：A：分层处理。 简单问题（80%）→ RAG 快速路径（< 1s） 复杂问题（20%）→ Agent 多步执行（3-10… —— 排查思路是什么？ {#q13}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：A：分层处理。 简单问题（80%）→ RAG 快速路径（< 1s） 复杂问题（20%）→ Agent 多步执行（3-10s） 用户看到的是：大部分情况秒回，偶尔需要等待。

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q14. 生产环境 从 RAG 走向 Agent 出现：A：三层保障。 1. 确定性工具：计算、查询用确定性代码 2. LLM 只做决策：不让 LLM 生成最终数据 3. 结果… —— 排查思路是什么？ {#q14}

| 维度 | 说明 |
|------|------|
| 类型 | 排障题 |
| 难度 | 中等 |
| 考点 | 线上排障 |

**参考答法（开发向）：**

**现象摘要**：A：三层保障。 1. 确定性工具：计算、查询用确定性代码 2. LLM 只做决策：不让 LLM 生成最终数据 3. 结果验证：执行后检查是否符合预期

**排查顺序**：
1. **范围**：影响面、开始时间、是否可回滚；
2. **证据链**：请求 ID → 各 hop 日志（网关 / 应用 / 向量库 / 模型 / 工具）；
3. **分层假设**：数据污染、配置变更、依赖超时、逻辑 bug——每次只验证一个；
4. **止血**：降级（缓存答案、缩小 top_k、关闭非关键工具）、限流；
5. **根因与回归**：补集成测试 + 告警阈值。

**常见追问：**

- 如何写一份给 on-call 的 runbook？
- 哪些信息必须打进结构化日志？

---

### Q15. RAG 和 Agent 有什么区别？在 从 RAG 走向 Agent 里如何选型？ {#q15}

| 维度 | 说明 |
|------|------|
| 类型 | 对比题 |
| 难度 | 中等 |
| 考点 | 对比 · RAG vs Agent |

**参考答法（开发向）：**

| 维度 | RAG | Agent |
|------|----------|----------|
| 定义 | 检索增强生成，先查资料再让模型回答。 | 能根据目标自主多步决策并调用工具的系统。 |
| 适用 | 见具体场景约束 | 见具体场景约束 |

**选型建议**：
- 用 **输入形态**（精确词 vs 语义改写）、**延迟预算**、**运维成本** 三张表说话；
- 给出「默认选谁 + 何时叠加谁」，必要时 Hybrid；
- 用评测集证明，而不是团队偏好。

**常见追问：**

- 能否画一张两路召回合并的时序图？
- 线上如何做 A/B？

---

### Q16. 从 RAG 走向 Agent 能力要上生产，你会优先保证哪三件事？ {#q16}

| 维度 | 说明 |
|------|------|
| 类型 | 生产化 |
| 难度 | 高频 |
| 考点 | 生产落地 |

**参考答法（开发向）：**

结合 从 RAG 走向 Agent 常见风险点：读下图时，先看「从 RAG 走向 Agent核心思想」建立直觉。

**推荐三件事**：
1. **SLO 与降级**：定义 P95/P99、错误预算；检索失败/模型超时时的兜底路径；
2. **可观测**：trace_id 贯穿；记录检索命中、token、工具调用、人工介入；
3. **变更安全**：配置与 Prompt 版本化；灰度 + 自动回滚条件；回归评测门禁。

面试时用 **一个真实指标** 收尾（如 citation 准确率、empty retrieval 率）。

**常见追问：**

- 如何做成本归因到租户/业务线？
- on-call 值班最需要哪三张大盘？

---

### Q17. 用 2 分钟讲清楚 从 RAG 走向 Agent 的技术方案（面向面试官）。 {#q17}

| 维度 | 说明 |
|------|------|
| 类型 | 综合题 |
| 难度 | 高频 |
| 考点 | 技术表述 |

**参考答法（开发向）：**

**30 秒版**：问题背景 → 核心组件 → 一条量化结果。

**2 分钟版**（可按链路讲）：它解决什么问题 → RAG 的天花板 → 从 RAG 到 Agent 的跨越 → 核心概念：RAG 和 Agent 的本质区别

每层补一句：**输入输出 + 失败怎么办**。
避免背章节标题；用「请求进来以后……」的叙事更自然。

**常见追问：**

- 如果面试官只给白板，你会先画哪张图？
- 你最自豪的一次优化是什么？

---

## 自测清单

- [ ] 能画主链路数据流（请求 → 核心服务 → 依赖 → 返回）
- [ ] 能举 1 个线上/d demo 中的真实案例
- [ ] 能说出 2 个失败模式 + 排查命令/日志字段
- [ ] 能讲清与相邻技术栈的分工（不要大包大揽）

**复习正文**：[214.rag-to-agent-transition-tutorial.md](../214.rag-to-agent-transition-tutorial.md)
